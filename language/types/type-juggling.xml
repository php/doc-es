<?xml version="1.0" encoding="utf-8"?>
<!-- EN-Revision: 50f76f26914c5a9e61aa26f2e36c4acb362a48fd Maintainer: PhilDaiguille Status: ready -->
<!-- Reviewed: no -->
<sect1 xml:id="language.types.type-juggling">
 <title>Manipulación de tipos</title>

 <simpara>
  PHP no requiere una definición explícita de tipo en la declaración de variables.
  En este caso, el tipo de una variable se determina por el valor que almacena.
  Es decir, si se asigna un <type>string</type> a la variable
  <varname>$var</varname>, entonces <varname>$var</varname> será de tipo
  <type>string</type>. Si posteriormente se asigna un valor de tipo
  <type>int</type> a <varname>$var</varname>, pasará a ser de tipo
  <type>int</type>.
 </simpara>

 <para>
  PHP puede intentar convertir automáticamente el tipo de un valor a otro en
  ciertos contextos. Los diferentes contextos que existen son:
  <itemizedlist>
   <listitem>
    <simpara>Numérico</simpara>
   </listitem>
   <listitem>
    <simpara>Cadena</simpara>
   </listitem>
   <listitem>
    <simpara>Lógico</simpara>
   </listitem>
   <listitem>
    <simpara>Entero y cadena</simpara>
   </listitem>
   <listitem>
    <simpara>Comparativo</simpara>
   </listitem>
   <listitem>
    <simpara>Función</simpara>
   </listitem>
  </itemizedlist>
 </para>

 <note>
  <simpara>
   Cuando un valor necesita ser interpretado como un tipo diferente, el valor
   en sí <emphasis>no</emphasis> cambia de tipo.
  </simpara>
 </note>

 <simpara>
  Para forzar a que una variable sea evaluada como un tipo determinado, consulte
  la sección sobre <link linkend="language.types.typecasting">moldeado de tipos</link>.
  Para cambiar el tipo de una variable, consulte la función
  <function>settype</function>.
 </simpara>

 <sect2>
  <title>Contextos numéricos</title>

  <simpara>
   Este es el contexto cuando se utiliza un
   <link linkend="language.operators.arithmetic">operador aritmético</link>.
  </simpara>

  <simpara>
   En este contexto, si alguno de los operandos es de tipo <type>float</type>
   (o no puede interpretarse como un <type>int</type>), ambos operandos se
   interpretarán como <type>float</type>s, y el resultado también será un
   <type>float</type>. De lo contrario, los operandos se interpretarán como
   <type>int</type>s, y el resultado también será un <type>int</type>.
   A partir de PHP 8.0.0, si uno de los operandos no puede interpretarse, se
   lanzará un <classname>TypeError</classname>.
  </simpara>
 </sect2>

 <sect2>
  <title>Contextos de cadena</title>

  <simpara>
   Este es el contexto cuando se utiliza <function>echo</function>,
   <function>print</function>,
   <link linkend="language.types.string.parsing">interpolación de cadena</link>,
   o el
   <link linkend="language.operators.string">operador de concatenación de
   cadena</link>.
  </simpara>

  <simpara>
   En este contexto, el valor se interpretará como <type>string</type>.
   Si el valor no puede interpretarse, se lanzará un
   <classname>TypeError</classname>. Antes de PHP 7.4.0, se generaba un
   <constant>E_RECOVERABLE_ERROR</constant>.
  </simpara>
 </sect2>

 <sect2>
  <title>Contextos lógicos</title>

  <simpara>
   Este es el contexto cuando se utilizan sentencias condicionales, el
   <link linkend="language.operators.comparison.ternary">operador ternario</link>,
   o un <link linkend="language.operators.logical">operador lógico</link>.
  </simpara>

  <simpara>
   En este contexto, el valor se interpretará como <type>bool</type>.
  </simpara>
 </sect2>

 <sect2>
  <title>Contextos entero y cadena</title>

  <simpara>
   Este es el contexto cuando se utilizan
   <link linkend="language.operators.bitwise">operadores a nivel de bits</link>.
  </simpara>

  <simpara>
   En este contexto, si todos los operandos son de tipo <type>string</type>,
   el resultado también será un <type>string</type>.
   De lo contrario, los operandos se interpretarán como <type>int</type>s,
   y el resultado también será un <type>int</type>.
   A partir de PHP 8.0.0, si uno de los operandos no puede interpretarse, se
   lanzará un <classname>TypeError</classname>.
  </simpara>
 </sect2>

 <sect2>
  <title>Contextos comparativos</title>

  <simpara>
   Este es el contexto cuando se utiliza un
   <link linkend="language.operators.comparison">operador de comparación</link>.
  </simpara>

  <simpara>
   Las conversiones de tipo que ocurren en este contexto se explican en la
   tabla de Comparación con diversos tipos
   <link linkend="language.operators.comparison.types">tabla</link>.
  </simpara>
 </sect2>

 <sect2 xml:id="language.types.type-juggling.function">
  <title>Contextos de función</title>

  <simpara>
   Este es el contexto cuando un valor se pasa a un parámetro tipado, propiedad
   o se devuelve desde una función que declara un tipo de retorno.
  </simpara>

  <para>
   En este contexto, el valor debe ser de ese tipo. Existen dos excepciones:
   la primera es: si el valor es de tipo <type>int</type> y el tipo declarado
   es <type>float</type>, entonces el entero se convierte en un número de
   punto flotante. La segunda es: si el tipo declarado es un tipo
   <emphasis>escalar</emphasis>
   <!-- e.g. An object that implements __toString will pass a string type -->
   , el valor es convertible a un tipo escalar y el modo de tipado coercitivo
   está activo (por omisión), el valor puede convertirse en un valor escalar
   aceptado. Consulte a continuación para obtener una descripción de este
   comportamiento.
  </para>

  <warning>
   <simpara>
    Las <link linkend="functions.internal">funciones internas</link> convierten
    automáticamente &null; a tipos escalares; este comportamiento está
    <emphasis>DEPRECADO</emphasis> a partir de PHP 8.1.0.
   </simpara>
  </warning>

  <sect3 xml:id="language.types.type-juggling.function.simple">
   <title>Tipado coercitivo con declaraciones de tipo simples</title>
   <itemizedlist>
    <listitem>
     <simpara>
      Declaración de tipo <type>bool</type>: el valor se interpreta como
      <type>bool</type>.
     </simpara>
    </listitem>
    <listitem>
     <simpara>
      Declaración de tipo <type>int</type>: el valor se interpreta como
      <type>int</type> si la conversión está bien definida. Por ejemplo, la
      cadena es <link linkend="language.types.numeric-strings">numérica</link>.
     </simpara>
    </listitem>
    <listitem>
     <simpara>
      Declaración de tipo <type>float</type>: el valor se interpreta como
      <type>float</type> si la conversión está bien definida. Por ejemplo, la
      cadena es <link linkend="language.types.numeric-strings">numérica</link>.
     </simpara>
    </listitem>
    <listitem>
     <simpara>
      Declaración de tipo <type>string</type>: el valor se interpreta como
      <type>string</type>.
     </simpara>
    </listitem>
   </itemizedlist>
  </sect3>

  <sect3 xml:id="language.types.type-juggling.function.union">
   <title>Tipado coercitivo con tipos de unión</title>
   <para>
    Cuando <literal>strict_types</literal> no está habilitado, las
    declaraciones de tipo escalar están sujetas a conversiones implícitas
    limitadas. Si el tipo exacto del valor no es parte de la unión, entonces
    el tipo objetivo se elige en el siguiente orden de preferencia:

    <orderedlist>
     <listitem>
      <simpara>
       <type>int</type>
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       <type>float</type>
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       <type>string</type>
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       <type>bool</type>
      </simpara>
     </listitem>
    </orderedlist>

    Si el tipo existe en la unión y el valor puede ser coercionado al tipo
    bajo la semántica de verificación de tipos existente de PHP, entonces se
    elige el tipo. De lo contrario, se prueba con el siguiente tipo.
   </para>

   <caution>
    <para>
     Como excepción, si el valor es una cadena y tanto int como float son parte
     de la unión, el tipo preferido se determina por la semántica de
     <link linkend="language.types.numeric-strings">cadena numérica</link>
     existente. Por ejemplo, para <literal>"42"</literal> se elige
     <type>int</type>, mientras que para <literal>"42.0"</literal> se elige
     <type>float</type>.
    </para>
   </caution>

   <note>
    <para>
     Los tipos que no forman parte de la lista de preferencias anterior no son
     objetivos elegibles para coerción implícita. En particular, no ocurren
     coerciones implícitas a los tipos <type>null</type>, <type>false</type>
     y <type>true</type>.
    </para>
   </note>

   <example>
    <title>Ejemplo de tipos siendo coercionados a un tipo parte de la unión</title>
    <programlisting role="php">
<![CDATA[
<?php
// int|string
42    --> 42          // tipo exacto
"42"  --> "42"        // tipo exacto
new ObjectWithToString --> "Resultado de __toString()"
                      // objeto nunca compatible con int, retrocede a string
42.0  --> 42          // float compatible con int
42.1  --> 42          // float compatible con int
1e100 --> "1.0E+100"  // float demasiado grande para int, retrocede a string
INF   --> "INF"       // float demasiado grande para int, retrocede a string
true  --> 1           // bool compatible con int
[]    --> TypeError   // array no compatible con int o string

// int|float|bool
"45"    --> 45        // cadena numérica int
"45.0"  --> 45.0      // cadena numérica float

"45X"   --> true      // no es cadena numérica, retrocede a bool
""      --> false     // no es cadena numérica, retrocede a bool
"X"     --> true      // no es cadena numérica, retrocede a bool
[]      --> TypeError // array no compatible con int, float o bool
?>
]]>
    </programlisting>
   </example>
  </sect3>

 </sect2>

 <sect2 xml:id="language.types.typecasting">
  <title>Moldeado de tipos</title>

  <simpara>
   El moldeado de tipos convierte el valor a un tipo elegido escribiendo el
   tipo entre paréntesis antes del valor a convertir.
  </simpara>

  <example>
   <title>Moldeado de tipos</title>
   <programlisting role="php">
<![CDATA[
<?php
$foo = 10;          // $foo es un entero
$bar = (bool) $foo; // $bar es un booleano

var_dump($bar);
?>
]]>
   </programlisting>
  </example>

  <simpara>
   Los moldeados permitidos son:
  </simpara>

  <simplelist>
   <member><literal>(int)</literal> - moldear a <type>int</type></member>
   <member><literal>(bool)</literal> - moldear a <type>bool</type></member>
   <member><literal>(float)</literal> - moldear a <type>float</type></member>
   <member><literal>(string)</literal> - moldear a <type>string</type></member>
   <member><literal>(array)</literal> - moldear a <type>array</type></member>
   <member><literal>(object)</literal> - moldear a <type>object</type></member>
   <member><literal>(unset)</literal> - moldear a <type>NULL</type></member>
  </simplelist>

  <simpara>
   El moldeado <link linkend="language.types.void.casting"><literal>(void)</literal></link>
   también está disponible a partir de PHP 8.5.0, pero no es una conversión
   de valor. Se utiliza como una sentencia para descartar explícitamente el
   resultado de una expresión.
  </simpara>

  <warning>
   <para>
    <literal>(integer)</literal> es un alias del moldeado <literal>(int)</literal>.
    <literal>(boolean)</literal> es un alias del moldeado <literal>(bool)</literal>.
    <literal>(binary)</literal> es un alias del moldeado <literal>(string)</literal>.
    <literal>(double)</literal> y <literal>(real)</literal> son alias del
    moldeado <literal>(float)</literal>. Estos moldeados no utilizan el nombre
    canónico del tipo y están deprecados a partir de PHP 8.5.0.
   </para>
  </warning>

  <warning>
   <simpara>
    El alias de moldeado <literal>(real)</literal> fue deprecado a partir de
    PHP 7.4.0 y eliminado a partir de PHP 8.0.0.
   </simpara>
  </warning>

  <warning>
   <simpara>
    El moldeado <literal>(unset)</literal> fue deprecado a partir de PHP 7.2.0.
    Tenga en cuenta que el moldeado <literal>(unset)</literal> es igual a
    asignar el valor <type>NULL</type> a la variable o llamada.
    El moldeado <literal>(unset)</literal> fue eliminado a partir de PHP 8.0.0.
   </simpara>
  </warning>

  <caution>
   <simpara>
    El moldeado <literal>(binary)</literal> y el prefijo <literal>b</literal>
    existen para soporte futuro. Actualmente <literal>(binary)</literal> y
    <literal>(string)</literal> son idénticos, sin embargo esto puede cambiar y
    no debe ser utilizado como dependencia.
   </simpara>
  </caution>

  <note>
   <para>
    Los espacios en blanco se ignoran dentro de los paréntesis de un moldeado.
    Por lo tanto, los siguientes dos moldeados son equivalentes:
    <informalexample>
     <programlisting role="php">
<![CDATA[
<?php
$foo = (int) $bar;
$foo = ( int ) $bar;
?>
]]>
     </programlisting>
    </informalexample>
   </para>
  </note>

  <informalexample>
   <simpara>
    Moldeando literales <type>string</type>s y variables a
    <type>string</type>s binarios:
   </simpara>

   <programlisting role="php">
<![CDATA[
<?php
$binary = (binary) $string;
$binary = b"cadena binaria";
?>
]]>
   </programlisting>
  </informalexample>

  <!-- TODO Remove or move into string context section? -->
  <simpara>
   En lugar de moldear una variable a un <type>string</type>, también es
   posible encerrar la variable entre comillas dobles.
  </simpara>

  <example>
   <title>Diferentes mecanismos de moldeado</title>
   <programlisting role="php">
<![CDATA[
<?php
$foo = 10;            // $foo es un entero
$str = "$foo";        // $str es una cadena
$fst = (string) $foo; // $fst también es una cadena

// Esto imprime que "son iguales"
if ($fst === $str) {
    echo "son iguales", PHP_EOL;
}
?>
]]>
   </programlisting>
  </example>

  <para>
   Puede que no sea obvio exactamente qué ocurrirá al moldear entre ciertos
   tipos. Para más información, consulte estas secciones:
   <simplelist>
    <member><link linkend="language.types.boolean.casting">Conversión a booleano</link></member>
    <member><link linkend="language.types.integer.casting">Conversión a entero</link></member>
    <member><link linkend="language.types.float.casting">Conversión a float</link></member>
    <member><link linkend="language.types.string.casting">Conversión a cadena</link></member>
    <member><link linkend="language.types.array.casting">Conversión a array</link></member>
    <member><link linkend="language.types.object.casting">Conversión a objeto</link></member>
    <member><link linkend="language.types.resource.casting">Conversión a recurso</link></member>
    <member><link linkend="language.types.null.casting">Conversión a NULL</link></member>
    <member><link linkend="language.types.void.casting">Descartar un valor con <literal>(void)</literal></link></member>
    <member><link linkend="types.comparisons">Las tablas de comparación de tipos</link></member>
   </simplelist>
  </para>

  <note>
   <simpara>
    Dado que PHP soporta indexación en <type>string</type>s mediante offsets
    utilizando la misma sintaxis que el indexado en <type>array</type>, el
    siguiente ejemplo se cumple para todas las versiones de PHP:
   </simpara>

   <example>
    <title>Usando offset de array con una cadena</title>
    <programlisting role="php">
<![CDATA[
<?php
$a    = 'car'; // $a es una cadena
$a[0] = 'b';   // $a sigue siendo una cadena
echo $a;       // bar
?>
]]>
    </programlisting>
   </example>

   <simpara>
    Consulte la sección titulada <link linkend="language.types.string.substr">Acceso a cadena por carácter</link>
    para más información.
   </simpara>
  </note>
 </sect2>

</sect1>
<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
