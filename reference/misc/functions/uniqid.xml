<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision: 1.3 $ -->
<!-- EN-Revision: 1.9 Maintainer: lboshell Status: ready -->
<!-- splitted from ./en/functions/misc.xml, last change in rev 1.2 -->
  <refentry id="function.uniqid">
   <refnamediv>
    <refname>uniqid</refname>
    <refpurpose>Generar un ID único</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Descripción</title>
     <methodsynopsis>
      <type>string</type><methodname>uniqid</methodname>
      <methodparam choice="opt"><type>string</type><parameter>prefijo</parameter></methodparam>
      <methodparam choice="opt"><type>bool</type><parameter>mas_entropia</parameter></methodparam>
     </methodsynopsis>
    <simpara>
     <function>uniqid</function> devuelve un identificador único con
     prefijo, basado en la hora actual en
     microsegundos. <parameter>prefijo</parameter> se hizo opcional en
     PHP 5 pero puede resultar útil, por ejemplo, si se generan
     identificadores simultáneamente en varias máquinas que pueden
     estar generando el identificador en el mismo microsegundo. En
     versiones de PHP hasta 4.3.1, <parameter>prefijo</parameter> sólo
     podía tener una longitud máxima de 114 caracteres.
    </simpara>
    <simpara>
     Si el parámetro opcional <parameter>mas_entropia</parameter> es
     &true;, <function>uniqid</function> agregará entropía adicional
     (usando el generador de congruencia lineal combinado) al final
     del valor de retorno, lo cual puede hacer más únicos los
     resultados.
    </simpara>
    <simpara>
     Con un <parameter>prefijo</parameter> vacío, la cadena devuelta
     tendrá una longitud de 13 caracteres. Si
     <parameter>mas_entropia</parameter> es &true;, tendrá 23
     caracteres.
    </simpara>
    <note>
     <simpara>
      El parámetro <parameter>prefijo</parameter> es requerido antes
      de PHP 5.
     </simpara>
    </note>
    <para>
     Si necesita un identificador o lexema único e intenta entregar
     ese valor al usuario a través de la red (mediante cookies de
     sesión), es recomendable que use algo como lo siguiente:
    </para>
    <para>
     <informalexample>
      <programlisting role="php">
<![CDATA[
<?php
// sin prefijo
// funciona únicamente en PHP 5 y versiones posteriores
$token = md5(uniqid());

// mejor, difícil de adivinar
$mejor_token = md5(uniqid(rand(), true));
?>
]]>
      </programlisting>
     </informalexample>
    </para>
    <simpara>
     Esto creará un identificar de 32 caracteres (un número
     hexadecimal de 128 bits) que es extremadamente difícil de
     predecir.
    </simpara>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
