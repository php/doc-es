<?xml version="1.0" encoding="utf-8"?>
<!-- EN-Revision: 75a76afb61d91f04feebc83931c5412b13682d2b Maintainer: PhilDaiguille Status: ready -->
<!-- Reviewed: no -->
<refentry xml:id="function.flock" xmlns="http://docbook.org/ns/docbook">
 <refnamediv>
  <refname>flock</refname>
  <refpurpose>Bloqueo de ficheros portátil y consultivo</refpurpose>
 </refnamediv>

 <refsect1 role="description">
  &reftitle.description;
  <methodsynopsis>
   <type>bool</type><methodname>flock</methodname>
   <methodparam><type>resource</type><parameter>stream</parameter></methodparam>
   <methodparam><type>int</type><parameter>operation</parameter></methodparam>
   <methodparam choice="opt"><type>int</type><parameter role="reference">would_block</parameter><initializer>&null;</initializer></methodparam>
  </methodsynopsis>
  <para>
   <function>flock</function> permite realizar un modelo simple de lectura/escritura
   que puede usarse en casi cualquier plataforma (incluyendo la mayoría de derivados
   de Unix e incluso Windows).
  </para>
  <para>
   El bloqueo también se libera mediante <function>fclose</function>,
   o cuando el <parameter>stream</parameter> es recolectado por el recolector de basura.
  </para>
  <para>
   PHP soporta una forma portátil de bloquear ficheros completos de manera consultiva
   (lo que significa que todos los programas que acceden deben usar el mismo método
   de bloqueo o no funcionará). Por omisión, esta función bloqueará hasta que se
   adquiera el bloqueo solicitado; esto puede controlarse con la opción
   <constant>LOCK_NB</constant> documentada más abajo.
  </para>
 </refsect1>

 <refsect1 role="parameters">
  &reftitle.parameters;
  <para>
   <variablelist>
    <varlistentry>
     <term><parameter>stream</parameter></term>
     <listitem>
      &fs.file.pointer;
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><parameter>operation</parameter></term>
     <listitem>
      <para>
       <parameter>operation</parameter> es uno de los siguientes:
       <itemizedlist>
        <listitem>
         <simpara>
          <constant>LOCK_SH</constant> para adquirir un bloqueo compartido (lector).
         </simpara>
        </listitem>
        <listitem>
         <simpara>
          <constant>LOCK_EX</constant> para adquirir un bloqueo exclusivo (escritor).
         </simpara>
        </listitem>
        <listitem>
         <simpara>
          <constant>LOCK_UN</constant> para liberar un bloqueo (compartido o exclusivo).
         </simpara>
        </listitem>
       </itemizedlist>
      </para>
      <para>
       También es posible añadir <constant>LOCK_NB</constant> como máscara de bits a una
       de las operaciones anteriores, si <function>flock</function> no debe
       bloquear durante el intento de bloqueo.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><parameter>would_block</parameter></term>
     <listitem>
      <para>
       El tercer argumento opcional se establece a 1 si el bloqueo bloquearía
       (condición de error EWOULDBLOCK).
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </para>
 </refsect1>

 <refsect1 role="returnvalues">
  &reftitle.returnvalues;
  <para>
   &return.success;
  </para>
 </refsect1>

 <refsect1 role="examples">
  &reftitle.examples;
  <para>
   <example>
    <title>Ejemplo de <function>flock</function></title>
    <programlisting role="php">
<![CDATA[
<?php

$fp = fopen("/tmp/lock.txt", "r+");

if (flock($fp, LOCK_EX)) {  // adquirir un bloqueo exclusivo
    ftruncate($fp, 0);      // truncar el fichero
    rewind($fp);
    fwrite($fp, "Escribir algo aquí\n");
    fflush($fp);            // volcar la salida antes de liberar el bloqueo
    flock($fp, LOCK_UN);    // liberar el bloqueo
} else {
    echo "¡No se pudo obtener el bloqueo!";
}

fclose($fp);

?>
]]>
    </programlisting>
   </example>
  </para>
  <para>
   <example>
    <title><function>flock</function> usando la opción <constant>LOCK_NB</constant></title>
    <programlisting role="php">
<![CDATA[
<?php
$fp = fopen('/tmp/lock.txt', 'r+');

/* Activar la opción LOCK_NB en una operación LOCK_EX */
if(!flock($fp, LOCK_EX | LOCK_NB)) {
    echo 'No se pudo obtener el bloqueo';
    exit(-1);
}

/* ... */

fclose($fp);
?>
]]>
    </programlisting>
   </example>
  </para>
 </refsect1>

 <refsect1 role="notes">
  &reftitle.notes;
  <note>
   <para>
    <function>flock</function> usa bloqueo obligatorio en lugar de bloqueo consultivo
    en Windows. El bloqueo obligatorio también es compatible en Linux y sistemas
    operativos basados en System V mediante el mecanismo habitual soportado por
    la llamada al sistema fcntl(): es decir, si el fichero en cuestión tiene
    establecido el bit de permiso setgid y el bit de ejecución del grupo desactivado.
    En Linux, el sistema de ficheros también deberá montarse con la opción mand
    para que esto funcione.
   </para>
  </note>
  <note>
   <para>
    Dado que <function>flock</function> requiere un puntero a fichero, puede que
    necesite usar un fichero de bloqueo especial para proteger el acceso a un fichero
    que intente truncar abriéndolo en modo escritura (con un argumento "w" o "w+"
    a <function>fopen</function>).
   </para>
  </note>
  <note>
   <para>
    Solo puede usarse en punteros a ficheros devueltos por <function>fopen</function>
    para ficheros locales, o punteros a ficheros que apunten a flujos en espacio
    de usuario que implementen el método <function>streamWrapper::stream_lock</function>.
   </para>
  </note>
  <warning>
   <para>
    Asignar otro valor al argumento <parameter>stream</parameter> en el código
    posterior liberará el bloqueo.
   </para>
  </warning>
  <warning>
   <para>
    En algunos sistemas operativos, <function>flock</function> se implementa a nivel
    de proceso. ¡Cuando se usa una API de servidor multihilo, puede que no se
    pueda confiar en <function>flock</function> para proteger ficheros contra otros
    scripts PHP que se ejecuten en paralelo en la misma instancia del servidor!
   </para>
   <para>
    <function>flock</function> no es compatible con sistemas de ficheros
    anticuados como <literal>FAT</literal> y sus derivados, y por lo tanto
    siempre devolverá &false; en estos entornos.
   </para>
  </warning>
  <note>
   <para>
    En Windows, si el proceso de bloqueo abre el fichero una segunda vez, no podrá
    acceder al fichero a través de este segundo manejador hasta que desbloquee el fichero.
   </para>
  </note>
 </refsect1>

</refentry>
<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
