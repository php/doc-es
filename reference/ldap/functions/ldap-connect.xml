<?xml version="1.0" encoding="iso-8859-1"?>
<!-- $Revision: 1.3 $ -->
<!-- EN-Revision: 1.5 Maintainer: javi Status: ready -->
<!-- splitted from ./en/functions/ldap.xml, last change in rev 1.2 -->
  <refentry id="function.ldap-connect">
   <refnamediv>
    <refname>ldap_connect</refname>
    <refpurpose>Conecta con un servidor LDAP</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Descripci&oacute;n</title>
     <methodsynopsis>
      <type>resource</type><methodname>ldap_connect</methodname>
      <methodparam choice="opt"><type>string</type><parameter>nombre_host</parameter></methodparam>
      <methodparam choice="opt"><type>int</type><parameter>puerto</parameter></methodparam>
     </methodsynopsis>
    <para>
     Devuelve un identificador de conexi&oacute;n positivo en caso de &eacute;xito, &oacute;
     falso si ocurre alg&uacute;n error. Cuando se emplea un servidor OpenLDAP versi&oacute;n 2.x.x, 
	 la funci&oacute;n <function>ldap_connect</function> siempre devuelve una variable de tipo 
	 <type>resource</type> ya que en realidad no se realiza la conexi&oacute;n, sino que se 
     inicializan los par&aacute;metros de conexi&oacute;n.
	 La conexi&oacute;n real se produce cuando se realiza la llamada a la siguiente funci&oacute;n 
	 ldap_* funcs, que normalmente es la funci&oacute;n <function>ldap_bind</function>.
	</para>
    <para> 
     <function>ldap_connect</function> establece una conexi&oacute;n con el
     servidor LDAP especificado en <parameter>nombre_host</parameter> y
     <parameter>puerto</parameter>.  Ambos argumentos son opcionales. Si
     no se especifican, se devuelve el identificador de la conexi&oacute;n LDAP actualmente
     abierta.  Si s&oacute;lo es especificado <parameter>nombre_host</parameter>
     el puerto tomado por defecto es el 389.</para>
	<para>
     Si se emplea un servidor OpenLDAP versi&oacute;n 2.x.x, se puede especificar una 
		URL en vez de un nombre de host. Para utilizar LDAP con SSL, se tiene que 
		compilar OpenLDAP con soporte SLL, configurar PHP para que utilice SSL y usar como 
		nombre de host "ldaps://hostname/". El par&aacute;metro <parameter>puerto</parameter> 
		no se tiene en cuenta cuando se utilizan URLs.
    </para>
    <note>
     <simpara>
      El soporte de URLs y de SLL se incluy&oacute; en la versi&oacute;n 4.0.4. 
     </simpara>
    </note>
    <example>
     <title>Ejemplo de conxi&oacute;n a un servidor LDAP.</title>
     <programlisting role="php">
<![CDATA[
<?php

// variables LDAP
$servidor_ldap = "ldap.example.com";  // el servidor LDAP al que se quiere conectar
$puerto_ldap   = 389;                 // el puerto al que se conectara

// Estableciendo la conexion con el servidor LDAP
$conexion_ldap = ldap_connect($servidor_ldap, $puerto_ldap) 
          or die("No ha sido posible conectarse al servidor $servidor_ldap");

?>
]]>
     </programlisting>
    </example>
    <example>
     <title>Ejemplo de conxi&oacute;n a un servidor LDAP seguro.</title>
     <programlisting role="php">
<![CDATA[
<?php

// Debe tenerse en cuenta que el sistema que quiere conectarse al servidor
// debe coincidir con el equipo al que se genero el certificado seguro 

$servidor_ldap = "ldaps://ldap.example.com/";

// Estableciendo la conexion con el servidor LDAP seguro
$conexion_ldap = ldap_connect($servidor_ldap) 
          or die("No ha sido posible conectarse al servidor {$servidor_ldap}");

?>
]]>
     </programlisting>
    </example>
    <para>
     Vea tambi&eacute;n <function>ldap_bind</function>.
    </para>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
