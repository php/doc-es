<?xml version="1.0" encoding="iso-8859-1"?>
<!-- $Revision: 1.3 $ -->
<!-- EN-Revision: 1.9 Maintainer: lboshell Status: ready -->
<!-- splitted from ./en/functions/dir.xml, last change in rev 1.2 -->
<refentry id="function.readdir">
 <refnamediv>
  <refname>readdir</refname>
  <refpurpose>Leer una entrada desde un gestor de
  directorio</refpurpose>
 </refnamediv>

 <refsect1 role="description">
  &reftitle.description;
  <methodsynopsis>
   <type>string</type><methodname>readdir</methodname>
   <methodparam><type>resource</type><parameter>gestor_dir</parameter></methodparam>
  </methodsynopsis>
  <para>
   Devuelve el nombre del siguiente archivo leido desde el
   directorio. Los nombres de archivo son devueltos en el orden en que
   son almacenados por el sistema de archivos.
  </para>
 </refsect1>

 <refsect1 role="parameters">
  &reftitle.parameters;
  <para>
   <variablelist>
    <varlistentry>
     <term><parameter>gestor_dir</parameter></term>
     <listitem>
      <para>
       El gestor de directorio tipo <type>resource</type> abierto
       previamente con <function>opendir</function>.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </para>
 </refsect1>

 <refsect1 role="returnvalues">
  &reftitle.returnvalues;
  <para>
   Devuelve el nombre de archivo en caso de &eacute;xito, o &false; si
   ocurre un error.
  </para>
 </refsect1>

 <refsect1 role="examples">
  &reftitle.examples;
  <para>
   <example>
    <title>Listar todos los archivos en un directorio</title>
    <para>
     Por favor note la forma en que el valor de retorno de
     <function>dir</function> es chequeado en los siguientes
     ejemplos. Se est&aacute; probando expl&iacute;citamente si el
     valor de retorno es id&eacute;ntico (igual y del mismo tipo
     que--vea los <link
     linkend="language.operators.comparison">Operadores de
     Comparaci&oacute;n</link> para m&aacute;s informaci&oacute;n) a
     &false; ya que de otra forma, cualquier entrada de directorio
     cuyo nombre eval&uacute;e a &false; detendr&aacute; el ciclo
     (p.ej. un directorio llamado "0").
    </para>
    <programlisting role="php">
<![CDATA[
<?php
// Note que !== no existia hasta 4.0.0-RC2

if ($gestor = opendir('/ruta/a/archivos')) {
    echo "Gestor de directorio: $gestor\n";
    echo "Archivos:\n";

    /* Esta es la forma correcto de iterar sobre el directorio. */
    while (false !== ($archivo = readdir($gestor))) {
        echo "$archivo\n";
    }

    /* Esta es la forma EQUIVOCADA de iterar sobre el directorio. */
    while ($archivo = readdir($gestor)) {
        echo "$archivo\n";
    }

    closedir($gestor);
}
?>
]]>
    </programlisting>
   </example>
  </para>
  <para>
   <example>
    <title>
     Listar todos los archivos en el directorio actual y eliminar
     <literal>.</literal> y <literal>..</literal>
    </title>
    <programlisting role="php">
<![CDATA[
<?php
if ($gestor = opendir('.')) {
    while (false !== ($archivo = readdir($gestor))) {
        if ($archivo != "." && $archivo != "..") {
            echo "$archivo\n";
        }
    }
    closedir($gestor);
}
?>
]]>
    </programlisting>
   </example>
  </para>
 </refsect1>

 <refsect1 role="seealso">
  &reftitle.seealso;
  <para>
   <simplelist>
    <member><function>is_dir</function></member>
    <member><function>glob</function></member>
   </simplelist>
  </para>
 </refsect1>
</refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
