<?xml version="1.0" encoding="iso-8859-1"?>
<!-- splitted from ./es/functions/network.xml, last change in rev 1.1 -->
<!-- last change to 'fsockopen' in en/ tree in rev 1.2 -->
  <refentry id="function.fsockopen">
   <refnamediv>
    <refname>fsockopen</refname>
    <refpurpose>Abre una conexi&oacute;n de dominio Internet o Unix via sockets.</refpurpose> 
   </refnamediv>
   <refsect1>
    <title>Descripci&oacute;n</title>
     <methodsynopsis>
     <type>int</type><methodname>fsockopen</methodname>
     <methodparam><type>string</type><parameter>hostname</parameter></methodparam>
     <methodparam><type>int</type><parameter>port</parameter></methodparam>
     <methodparam choice="opt"><type>int</type><parameter>errno</parameter></methodparam>
     <methodparam choice="opt"><type>string</type><parameter>errstr</parameter></methodparam>
     <methodparam choice="opt"><type>double</type><parameter>timeout</parameter></methodparam>
     </methodsynopsis>

    <para>
     Inicia una conexi&oacute;n de dominio Internet (AF_INET) o Unix (AF_UNIX).
     Para el domino Internet, abrir&aacute; una conexi&oacute;n TCP hacia el ordenador 
     <parameter>hostname</parameter> en el puerto <parameter>port</parameter>. Para
     el dominio Unix, <parameter>hostname</parameter> se usar&aacute; como ruta 
     al socket, <parameter>port</parameter> debe ser 0 para este caso.
     El par&aacute;metro opcional <parameter>timeout</parameter> se puede usar 
     para especificar un timeout en segundos para establecer la conexi&oacute;n.</para>

    <para>
     <function>fsockopen</function> devuelve un puntero a fichero, el cual 
     se puede usar junto con las otras funciones de ficheros (como
     <function>fgets</function>, <function>fgetss</function>,
     <function>fputs</function>, <function>fclose</function>,
     <function>feof</function>).</para>  
     
    <para>
     Si la llamada falla, esta devolver&aacute; falso y si los par&aacute;metros opcionales
     <parameter>errno</parameter> y <parameter>errstr</parameter> est&aacute;n 
     presentes, indicar&aacute;n el error del sistema que ocurri&oacute; en la 
     llamada connect(). Si errno es 0 y la funci&oacute;n devolvi&aacute; falso, 
     nos indica que el error ocurri&oacute; antes de la llamada connect(). 
     Esto es debido principalmente a problemas inicializando el socket. 
     Observe que los argumentos errno y errstr deben ser pasados por referencia.
    </para>

    <para>
     Dependiendo del entorno, el dominio Unix o el par&aacute;metro opcional,
     timeout puede no estar disponible.</para> 
     
	<para>
     Por defecto, el socket ser&aacute; abierto en modo de bloqueo. 
     Puede cambiarlo a modo de no bloqueo usando 
     <function>set_socket_blocking</function>.
	 <example>
     <title>ejemplo con fsockopen</title>
	  <programlisting>
$fp = fsockopen("www.php.net", 80, $errno, $errstr, 30);
if(!$fp) {
	echo "$errstr ($errno)&lt;br&gt;\n";
} else {
	fputs($fp,"GET / HTTP/1.0\n\n");
	while(!feof($fp)) {
		echo fgets($fp,128);
	}
	fclose($fp);
}
	  </programlisting>
	 </example>

     Ver tambi&eacute;n: <function>pfsockopen</function></para>
     
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
