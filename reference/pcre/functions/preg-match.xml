<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision: 290051 $ -->
<!-- EN-Revision: 7dbdeb80f52354fef58e707f4fb47800e8de56f1 Maintainer: seros Status: ready -->

<refentry xml:id="function.preg-match" xmlns="http://docbook.org/ns/docbook">
 <refnamediv>
  <refname>preg_match</refname>
  <refpurpose>Realiza una comparación con una expresión regular</refpurpose>
 </refnamediv>

 <refsect1 role="description">
  &reftitle.description;
  <methodsynopsis>
   <type>int</type><methodname>preg_match</methodname>
   <methodparam><type>string</type><parameter>patrón</parameter></methodparam>
   <methodparam><type>string</type><parameter>sujeto</parameter></methodparam>
   <methodparam choice="opt"><type>array</type><parameter role="reference">coincidencias</parameter></methodparam>
   <methodparam choice="opt"><type>int</type><parameter>banderas</parameter></methodparam>
   <methodparam choice="opt"><type>int</type><parameter>índice</parameter></methodparam>
  </methodsynopsis>
  <para>
   Busca en <parameter>sujeto</parameter> una coincidencia con la expresión regular
   dada en <parameter>patrón</parameter>.
  </para>
 </refsect1>

 <refsect1 role="parameters">
  &reftitle.parameters;
  <para>
   <variablelist>
    <varlistentry>
     <term><parameter>patrón</parameter></term>
     <listitem>
      <para>
       El patrón de búsqueda, como cadena.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><parameter>sujeto</parameter></term>
     <listitem>
      <para>
       La cadena de entrada.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><parameter>coincidencias</parameter></term>
     <listitem>
      <para>
       Si se proporciona <parameter>coincidencias</parameter>, entonces éste se llena con
       los resultados de la búsqueda. <varname>$coincidencias[0]</varname> contendrá el
       texto que coincidió con el patrón completo, <varname>$coincidencias[1]</varname>
       tendrá el texto que coincidió con el primer sub-patrón entre paréntesis capturado,
       y así sucesivamente.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><parameter>banderas</parameter></term>
     <listitem>
      <para>
       <parameter>banderas</parameter> puede estar seguido de la siguiente bandera:
       <variablelist>
        <varlistentry>
         <term><constant>PREG_OFFSET_CAPTURE</constant></term>
         <listitem>
          <simpara>
           Si se pasa esta bandera, por cada coincidencia producida, el índice de la cadena
           añadida también será devuelto. Observe que esto cambia el valor de
           <parameter>coincidencias</parameter> dentro de una matriz donde cada elemento es una
           matriz consistente en la cadena coincidente en el índice <literal>0</literal>
           y su índice dentro de la cadena <parameter>sujeto</parameter> en el índice
           <literal>1</literal>.
          </simpara>
         </listitem>
        </varlistentry>
       </variablelist>
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><parameter>índice</parameter></term>
     <listitem>
      <para>
       Normalmente, la búsqueda comienza por el principio de la cadena objetivo.
       El parámetro opcional <parameter>índice</parameter> se puede usar para
       especificar el lugar alternativo desde el cual comenzar la búsqueda (en bytes).
      </para>
      <note>
       <para>
        Usar <parameter>índice</parameter> no es equivalente a pasar
        <literal>substr($sujeto, $índice)</literal> a
        <function>preg_match</function> en lugar de la cadena objetivo,
        ya que <parameter>patrón</parameter> puede contener aserciones como
        <emphasis>^</emphasis>, <emphasis>$</emphasis> o
        <emphasis>(?&lt;=x)</emphasis>. Compare:
        <informalexample>
         <programlisting role="php">
<![CDATA[
<?php
$subject = "abcdef";
$pattern = '/^def/';
preg_match($pattern, $subject, $matches, PREG_OFFSET_CAPTURE, 3);
print_r($matches);
?>
]]>
         </programlisting>
         &example.outputs;
         <screen>
<![CDATA[
Array
(
)
]]>
         </screen>
         <para>
          mientras que este ejemplo
         </para>
         <programlisting role="php">
<![CDATA[
<?php
$subject = "abcdef";
$pattern = '/^def/';
preg_match($pattern, substr($subject,3), $matches, PREG_OFFSET_CAPTURE);
print_r($matches);
?>
]]>
         </programlisting>
         <para>
          producirá
         </para>
         <screen>
<![CDATA[
Array
(
    [0] => Array
        (
            [0] => def
            [1] => 0
        )

)
]]>
         </screen>
        </informalexample>
       </para>
      </note>
     </listitem>
    </varlistentry>
   </variablelist>
  </para>
 </refsect1>

 <refsect1 role="returnvalues">
  &reftitle.returnvalues;
  <para>
   <function>preg_match</function> devuelve el número de veces que
   <parameter>patrón</parameter> coincide. Esto pordrá ser 0 veces
   (sin coincidencias) o 1 vez ya que <function>preg_match</function> detendrá la
   búsqueda después de la primera coincidencia. <function>preg_match_all</function>,
   por el contrario, continuará hasta que alcance el final de
   <parameter>sujeto</parameter>.
   <function>preg_match</function> devuelve &false; si se produjo un error.
  </para>
 </refsect1>

 <refsect1 role="changelog">
  &reftitle.changelog;
  <para>
   <informaltable>
    <tgroup cols="2">
     <thead>
      <row>
       <entry>&Version;</entry>
       <entry>&Description;</entry>
      </row>
     </thead>
     <tbody>
      <row>
       <entry>5.2.2</entry>
       <entry>
        Los sub-patrones nominados ahora aceptan la
        sintaxis <literal>(?&lt;nombre&gt;)</literal>
        y <literal>(?'nombre')</literal> así como
        <literal>(?P&lt;nombre&gt;)</literal>. Las versiones anteriores
        sólo aceptaban <literal>(?P&lt;nombre&gt;)</literal>.
       </entry>
      </row>
      <row>
       <entry>4.3.3</entry>
       <entry>
        Se añadió el parámetro <parameter>índice</parameter>
       </entry>
      </row>
      <row>
       <entry>4.3.0</entry>
       <entry>
        Se añadió las bandera <constant>PREG_OFFSET_CAPTURE</constant>
       </entry>
      </row>
      <row>
       <entry>4.3.0</entry>
       <entry>
        Se añadió el parámetro <parameter>banderas</parameter>
       </entry>
      </row>
     </tbody>
    </tgroup>
   </informaltable>
  </para>
 </refsect1>

 <refsect1 role="examples">
  &reftitle.examples;
  <para>
   <example>
    <title>Busca la cadena de texto "php"</title>
    <programlisting role="php">
<![CDATA[
<?php
// La "i" después del delimitador de patrón indica una búsqueda
// sin tener en cuenta mayúsculas/minúsculas
if (preg_match("/php/i", "PHP is the web scripting language of choice.")) {
    echo "A match was found.";
} else {
    echo "A match was not found.";
}
?>
]]>
    </programlisting>
   </example>
  </para>
  <para>
   <example>
    <title>Busca la palabra "web"</title>
    <programlisting role="php">
<![CDATA[
<?php
/* El \b en el patrón indica un límite de palabra, por lo que sólo la palabra
 * definida "web" se compara, y no una palabra parcial como "webbing" o "cobweb" */
if (preg_match("/\bweb\b/i", "PHP is the web scripting language of choice.")) {
    echo "A match was found.";
} else {
    echo "A match was not found.";
}

if (preg_match("/\bweb\b/i", "PHP is the website scripting language of choice.")) {
    echo "A match was found.";
} else {
    echo "A match was not found.";
}
?>
]]>
    </programlisting>
   </example>
  </para>
  <para>
   <example>
    <title>Obtener el nombre de dominio de una URL</title>
    <programlisting role="php">
<![CDATA[
<?php
// obtiene el nombre del host de la URL
preg_match('@^(?:http://)?([^/]+)@i',
    "http://www.php.net/index.html", $matches);
$host = $matches[1];

// obtiene los dos últimos segmentos del nombre del host
preg_match('/[^.]+\.[^.]+$/', $host, $matches);
echo "domain name is: {$matches[0]}\n";
?>
]]>
    </programlisting>
    &example.outputs;
    <screen>
<![CDATA[
domain name is: php.net
]]>
    </screen>
   </example>
  </para>
  <para>
   <example>
    <title>Usar sub-patrones nominados</title>
    <programlisting role="php">
<![CDATA[
<?php

$str = 'foobar: 2008';

// Funciona en PHP 5.2.2 y posterior.
preg_match('/(?<name>\w+): (?<digit>\d+)/', $str, $matches);

// Antes de PHP 5.2.2, use esto:
// preg_match('/(?P<name>\w+): (?P<digit>\d+)/', $str, $matches);

print_r($matches);

?>
]]>
    </programlisting>
    &example.outputs;
    <screen>
<![CDATA[
Array
(
    [0] => foobar: 2008
    [name] => foobar
    [1] => foobar
    [digit] => 2008
    [2] => 2008
)
]]>
    </screen>
   </example>
  </para>
 </refsect1>

 <refsect1 role="notes">
  &reftitle.notes;
  <tip>
   <para>
    No use <function>preg_match</function> si solo quiere verificar si
    una cadena está contenida en otra cadena. Use
    <function>strpos</function> o <function>strstr</function> en su lugar ya que
    éstas serán más rápidas.
   </para>
  </tip>
 </refsect1>

 <refsect1 role="seealso">
  &reftitle.seealso;
  <para>
   <simplelist>
    <member><function>preg_match_all</function></member>
    <member><function>preg_replace</function></member>
    <member><function>preg_split</function></member>
   </simplelist>
  </para>
 </refsect1>

</refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
