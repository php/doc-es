<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision: 288721 $ -->
<!-- EN-Revision: af4410a7e15898c3dbe83d6ea38246745ed9c6fb Maintainer: seros Status: ready -->
<appendix xml:id="regex.examples">
 &reftitle.examples;
 <para>
  <example>
  <title>Ejemplos de Expresiones Regulares</title>
   <programlisting role="php">
<![CDATA[
<?php
// Devuelve true si "abc" se encuentra en cualquier lugar de $string.
ereg("abc", $string);            

// Devuelve true si "abc" se encuentra al comienzo de $string.
ereg("^abc", $string);

// Devuelve true si "abc" se encuentra al final de $string.
ereg("abc$", $string);

// Devuelve true si el navegador del cliente es Netscape 2, 3 o MSIE 3.
eregi("(ozilla.[23]|MSIE.3)", $_SERVER["HTTP_USER_AGENT"]);

// Coloca tres palabras separadas por un espacio dento de $regs[1], $regs[2] y $regs[3].
ereg("([[:alnum:]]+) ([[:alnum:]]+) ([[:alnum:]]+)", $string, $regs); 

// Pone una etiqueta <br /> al principio de $string.
$string = ereg_replace("^", "<br />", $string); 

// Pone una etiqueta <br /> al final de $string.
$string = ereg_replace("$", "<br />", $string); 

// Se deshace de cualquier carácter de nueva línea en $string.
$string = ereg_replace("\n", "", $string);
?>
]]>
   </programlisting>
  </example>
 </para>
</appendix>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->

