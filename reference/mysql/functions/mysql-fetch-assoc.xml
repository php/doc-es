<?xml version="1.0" encoding="iso-8859-1"?>
<!-- $Revision: 1.1 $ -->
<!-- EN-Revision: 1.8 Maintainer: lboshell Status: ready -->
<!-- splitted from ./en/functions/mysql.xml, last change in rev 1.45 -->
  <refentry id="function.mysql-fetch-assoc">
   <refnamediv>
    <refname>mysql_fetch_assoc</refname>
    <refpurpose>
     Recupera una fila de resultado como una matriz asociativa
    </refpurpose>
   </refnamediv>
   <refsect1>
    <title>Descripci&oacute;n</title>
     <methodsynopsis>
      <type>array</type><methodname>mysql_fetch_assoc</methodname>
      <methodparam><type>resource</type><parameter>resultado</parameter></methodparam>
     </methodsynopsis>
    <para>
Devuelve una matriz asociativa que corresponde a la fila recuperada, o
&false; si no hay m&aacute;s filas.
</para>
    <para>
<function>mysql_fetch_assoc</function> es equivalente a llamar
<function>mysql_fetch_array</function> con MYSQL_ASSOC como segundo
par&aacute;metro opcional. &Uacute;nicamente devuelve una matriz
asociativa. Esta es la forma en que originalmente trabajaba
<function>mysql_fetch_array</function>. Si necesita &iacute;ndices
num&eacute;ricos as&iacute; como asociativos, utilice
<function>mysql_fetch_array</function>.
    </para>
    <para>
Si dos o m&aacute;s columnas del resultado tienen los mismos nombres
de campo, la &uacute;ltima columna tomar&aacute; precedencia. Para
acceder a otras columnas con el mismo nombre, tendr&aacute; que
acceder al resultado con &iacute;ndices num&eacute;ricos mediante el
uso de <function>mysql_fetch_row</function> o agregar
sobrenombres. Vea el ejemplo en la descripci&oacute;n de
<function>mysql_fetch_array</function> respecto a los sobrenombres.
    </para>
    <para>
Algo importante a notar es que el uso de
<function>mysql_fetch_assoc</function> <emphasis>no es
significativamente</emphasis> m&aacute;s lento que el uso de
<function>mysql_fetch_row</function>, al mismo tiempo que provee un
valor agregado considerable.
    </para>
    &database.field-case;
    <para>
     <example>
      <title>Un ejemplo extendido de <function>mysql_fetch_assoc</function></title>
      <programlisting role="php">
<![CDATA[
<?php

    $conexion = mysql_connect("localhost", "mysql_user", "mysql_password");
    
    if (!$conexion) {
        echo "No pudo conectarse a la BD: " . mysql_error();
        exit;
    }
    
    if (!mysql_select_db("nombre_de_la_bd")) {
        echo "No ha sido posible seleccionar la BD: " . mysql_error();
        exit;
    }
    
    $sql = "SELECT id as id_usuario, nombre_completo, status_usuario 
            FROM   alguna_tabla
            WHERE  status_usuario = 1";

    $resultado = mysql_query($sql);

    if (!$resultado) {
        echo "No pudo ejecutarse satisfactoriamente la consulta ($sql) " .
             "en la DB: " . mysql_error();
        exit;
    }
    
    if (mysql_num_rows($resultado) == 0) {
        echo "No se han encontrado filas, nada a imprimir, asi que voy " .
             "a detenerme.";
        exit;
    }

    // Mientras exista una fila de datos, colocar esa fila en $fila
    // como una matriz asociativa
    // Nota: Si solo espera una fila, no hay necesidad de usar un ciclo
    // Nota: Si coloca extract($fila); dentro del siguiente ciclo,
    // estara creando $id_usuario, $nombre_completo, y $status_usuario

    while ($fila = mysql_fetch_assoc($resultado)) {
        echo $fila["id_usuario"];
        echo $fila["nombre_completo"];
        echo $fila["status_usuario"];
    }
	    
    mysql_free_result($resultado);

?>
]]>
      </programlisting>
     </example>
    </para>
    <para>
     Vea tambi&eacute;n
     <function>mysql_fetch_row</function>,
     <function>mysql_fetch_array</function>,
     <function>mysql_query</function> y
     <function>mysql_error</function>.
    </para>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
