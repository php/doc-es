<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision$ -->
<!-- EN-Revision: n/a Maintainer: baoengb Status: ready -->
<!-- splitted from ./en/functions/mysql.xml, last change in rev 1.62 -->
  <refentry xml:id="function.mysql-query" xmlns="http://docbook.org/ns/docbook">
   <refnamediv>
    <refname>mysql_query</refname>
    <refpurpose>Envía una consulta de MySQL</refpurpose>
   </refnamediv>

   <refsect1 role="description">
    &reftitle.description;
    <methodsynopsis>
     <type>resource</type><methodname>mysql_query</methodname>
     <methodparam><type>string</type><parameter>query</parameter></methodparam>
     <methodparam choice="opt"><type>resource</type><parameter>identificador_de_enlace</parameter></methodparam>
    </methodsynopsis>
    <para>
     <function>mysql_query</function> envía una consulta (a la base de
     datos activa en el servidor asociado con el
     <parameter>identificador_de_enlace</parameter> dado).
    </para>
   </refsect1>

   <refsect1 role="parameters">
    &reftitle.parameters;
    <para>
     <variablelist>
      <varlistentry>
       <term><parameter>query</parameter></term>
       <listitem>
        <para>
         Una consulta SQL
        </para>
        <para>
         La consulta no debe terminar con punto y coma.
        </para>
       </listitem>
      </varlistentry>
      <varlistentry>
       <term><parameter>identificador_de_enlace</parameter></term>
       <listitem>
        <para>
         Un identificador de enlace, como el regresado por
         <function>mysql_connect</function>.
        </para>
        <para>
         Si <parameter>identificador_de_enlace</parameter> no es específicado,
         se asume el último enlace abierto. Si no se ha abierto enlace,
         la función intenta establecer uno tal y como si se hubiera
         llamado a <function>mysql_connect</function> sin argumentos, y lo
         usará. El resultado de la consulta es puesto en la memoria 
         intermedia (buffer).
        </para>
       </listitem>
      </varlistentry>
     </variablelist>
    </para>
   </refsect1>

   <refsect1 role="returnvalues">
    &reftitle.returnvalues;
    <para>
     Para las sentencias SELECT, SHOW, DESCRIBE o EXPLAIN,
     <function>mysql_query</function> 
     regresa un <type>resource</type> en caso exitoso, y &false; en error.
    </para>
    <para>
     Para otro tipo de sentencia SQL, UPDATE, DELETE, DROP, etc,
     <function>mysql_query</function> regresa &true; en caso exitoso y
     &false; en error.
    </para>
    <para>
     El resultado obtenido debe ser pasado a 
     <function>mysql_fetch_array</function>, y otras funciones para
     el manejo de las tablas del resultado, para accesar los datos regresados.
    </para>
    <para>
     Use <function>mysql_num_rows</function> para encontrar cuantas filas 
     fueron regresadas para una sentencia SELECT o 
     <function>mysql_affected_rows</function> para encontrar cuantas filas 
     fueron afectadas por una sentencia DELETE, INSERT, REPLACE, o UPDATE.
    </para>
    <para>
     <function>mysql_query</function> también fallará y 
     regresará &false; si el usuario no tiene permiso de accesar la o
     las tablas referenciadas por la consulta.
    </para>
   </refsect1>

   <refsect1 role="examples">
    &reftitle.examples;
    <para>
     <example>
      <title>Consulta inválida</title>
      <para>
       La siguiente consulta es sintácticamente inv&aacute;lida de 
       tal manera que <function>mysql_query</function> falla y regresa &false;.
      </para>
      <programlisting role="php">
<![CDATA[
<?php
$result = mysql_query('SELECT * WHERE 1=1');
if (!$result) {
    die('Invalid query: ' . mysql_error());
}

?>
]]>
      </programlisting>
     </example>
    </para>
    <para>
     <example>
      <title>Consulta Válida</title>
      <para>
       La siguiente consulta es válida, así que 
       <function>mysql_query</function> regresa un  <type>resource</type>.
      </para>
      <programlisting role="php">
<![CDATA[
<?php
// This could be supplied by a user, for example
$firstname = 'fred';
$lastname  = 'fox';

// Formulate Query
// This is the best way to perform a SQL query
// For more examples, see mysql_real_escape_string()
$query = sprintf("SELECT firstname, lastname, address, age FROM friends WHERE firstname='%s' AND lastname='%s'",
    mysql_real_escape_string($firstname),
    mysql_real_escape_string($lastname));

// Perform Query
$result = mysql_query($query);

// Check result
// This shows the actual query sent to MySQL, and the error. Useful for debugging.
if (!$result) {
    $message  = 'Invalid query: ' . mysql_error() . "\n";
    $message .= 'Whole query: ' . $query;
    die($message);
}

// Use result
// Attempting to print $result won't allow access to information in the resource
// One of the mysql result functions must be used
// See also mysql_result(), mysql_fetch_array(), mysql_fetch_row(), etc.
while ($row = mysql_fetch_assoc($result)) {
    echo $row['firstname'];
    echo $row['lastname'];
    echo $row['address'];
    echo $row['age'];
}

// Free the resources associated with the result set
// This is done automatically at the end of the script
mysql_free_result($result);
?>
]]>
      </programlisting>
     </example>
    </para>
   </refsect1>

   <refsect1 role="seealso">
    &reftitle.seealso;
    <para>
     <simplelist>
      <member><function>mysql_connect</function></member>
      <member><function>mysql_error</function></member>
      <member><function>mysql_real_escape_string</function></member>
      <member><function>mysql_result</function></member>
      <member><function>mysql_fetch_assoc</function></member>
      <member><function>mysql_unbuffered_query</function></member>
     </simplelist>
    </para>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
