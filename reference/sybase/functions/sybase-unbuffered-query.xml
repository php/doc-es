<?xml version="1.0" encoding="iso-8859-1"?>
<!-- $Revision: 1.2 $ -->
<!-- EN-Revision: 1.6 Maintainer: baoengb Status: Ready -->

  <refentry id="function.sybase-unbuffered-query">
   <refnamediv>
    <refname>sybase_unbuffered_query</refname>
    <refpurpose>Ev&iacute;a una sentencia SQL a Sybase sin almacenamiento 
	 intermedio</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Descripci&oacute;n</title>
     <methodsynopsis>
      <type>int</type><methodname>sybase_unbuffered_query</methodname>
      <methodparam><type>string</type><parameter>setencia</parameter></methodparam>
      <methodparam><type>int</type><parameter>identificador_de_enlace</parameter></methodparam>
<methodparam choice="opt"><type>int</type><parameter>almacena_resultado</parameter></methodparam>
     </methodsynopsis>
    <para>
     Devuelve:Un identificador de resultado Sybase positivo en caso exitoso, o &false; ante un error.
    </para>
    <para> 
     <function>sybase_unbuffered_query</function> envía una consulta a la
	   actual base de datos activa en el servidor que está asociada con el 
		identificador de enlace especificado. Si no se especifica un 
		identificador de enlace, se asume el último enlace abierto. Si no hay un 
		enlace abierto, la función intentará establecer un enlace como si  <function>sybase_connect</function> fuese llamada, y lo usará. 

    </para>
    <para>
     A diferencia de <function>sybase_query</function>,
     <function>sybase_unbuffered_query</function> leer&aacute; s&oacute;lo la 
	  primera fila del resultado asociado. <function>sybase_fetch_array</function> una funci&oacute;n similar leer&aacute;
mas filas seg&uacute;n necesite.  <function>sybase_data_seek</function>
     lee hasta la fila solicitada.  Este comportamiento puede producir un 
	  mejor desempeño en resultados asociados muy grandes.
    </para>
    <para>
     <function>sybase_num_rows</function> S&oacute;lo regresar&aacute; el n&uacute;mero de filas del resultado asociado que ha sido leido. Para Sybase, 
el n&uacute;mero de filas es desconocido, por lo tanto debe ser calculado 
por una implementaci&oacute;n del cliente.
    </para>
    <note>
     <para>Si no ha utilizado todos los resultados asociados antes de
	  ejecutar una nueva sentencia, PHP generar&aacute; una advertencia y 
	  cancelar&aacute; todos los resultados pendientes.  Para evitar esto,
	  use <function>sybase_free_result</function> el cual cancelar&aacute;
	  los resultados pendientes de una sentencia sin almacenamiento 
	  intermedio.
     </para>
    </note>
    <para>
     El par&aacute;metro opcional <parameter>almacena_resultado</parameter> 
	  puede estar en &false; para indicar que el resultado asociado no debe ser 
	  traido a la memoria, y as&iacute; minimiza el uso de la memoria, 
	  lo cu&aacute;l es particularmente interesante con gran cantidad de 
	  filas en un resultado asociado.
    </para>
    <para>
     <example>
      <title>Ejemplo <function>sybase_unbuffered_query</function></title>
      <programlisting role="php">
<![CDATA[
<?php
       $dbh= sybase_connect('SYBASE', '', '');
       $q= sybase_unbuffered_query('select firstname, lastname from huge_table', $dbh, false);
       sybase_data_seek($q, 10000);
       $i= 0;
       while ($row= sybase_fetch_row($q)) {
               echo $row[0] . ' ' . $row[0];
               if ($i++ > 40000) break;
       }
       sybase_free_result($q);
       sybase_close($dbh);
?>
]]>
      </programlisting>
     </example>
    </para>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
