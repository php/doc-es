<?xml version="1.0" encoding="utf-8"?>
<!-- EN-Revision: 9f4cb232d01a06077a2324e38f767d63f87f2e5f Maintainer: PhilDaiguille Status: ready -->
<!-- Reviewed: no -->
<reference xml:id="class.mongodb-driver-cursor" role="class" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:xi="http://www.w3.org/2001/XInclude">

 <title>La clase MongoDB\Driver\Cursor</title>
 <titleabbrev>MongoDB\Driver\Cursor</titleabbrev>

 <partintro>

<!-- {{{ MongoDB\Driver\Cursor intro -->
  <section xml:id="mongodb-driver-cursor.intro">
   &reftitle.intro;
   <simpara>
    La clase <classname>MongoDB\Driver\Cursor</classname> encapsula
    los resultados de un comando o consulta de MongoDB y puede ser devuelta por
    <methodname>MongoDB\Driver\Manager::executeCommand</methodname> o
    <methodname>MongoDB\Driver\Manager::executeQuery</methodname>, respectivamente.
   </simpara>
  </section>
<!-- }}} -->

  <section xml:id="mongodb-driver-cursor.synopsis">
   &reftitle.classsynopsis;

<!-- {{{ Synopsis -->
   <classsynopsis>
    <ooclass><classname>MongoDB\Driver\Cursor</classname></ooclass>

<!-- {{{ Class synopsis -->
    <classsynopsisinfo>
     <modifier>final</modifier>
     <ooclass>
      <classname>MongoDB\Driver\Cursor</classname>
     </ooclass>

     <oointerface>
      <interfacename>MongoDB\Driver\CursorInterface</interfacename>
     </oointerface>
    </classsynopsisinfo>
<!-- }}} -->

    <classsynopsisinfo role="comment">&Methods;</classsynopsisinfo>
    <xi:include xpointer="xmlns(db=http://docbook.org/ns/docbook) xpointer(id('class.mongodb-driver-cursor')/db:refentry/db:refsect1[@role='description']/descendant::db:methodsynopsis[not(@role='procedural')])" />
   </classsynopsis>
<!-- }}} -->

  </section>

  <section role="changelog">
   &reftitle.changelog;
   <informaltable>
    <tgroup cols="2">
     <thead>
      <row>
       <entry>&Version;</entry>
       <entry>&Description;</entry>
      </row>
     </thead>
     <tbody>
      <row>
       <entry>PECL mongodb 1.9.0</entry>
       <entry>
        Implementa <interfacename>Iterator</interfacename>.
       </entry>
      </row>
      <row>
       <entry>PECL mongodb 1.6.0</entry>
       <entry>
        Implementa <interfacename>MongoDB\Driver\CursorInterface</interfacename>,
        que extiende <interfacename>Traversable</interfacename>.
       </entry>
      </row>
     </tbody>
    </tgroup>
   </informaltable>
  </section>

  <section xml:id="mongodb-driver-cursor.examples">
   &reftitle.examples;

   <example xml:id="mongodb-driver-cursor.examples.foreach">
    <title>Lectura de un conjunto de resultados</title>
    <simpara>
     <methodname>MongoDB\Driver\Manager::executeCommand</methodname> y
     <methodname>MongoDB\Driver\Manager::executeQuery</methodname> devuelven
     sus resultados como un objeto <classname>MongoDB\Driver\Cursor</classname>.
     Este objeto puede usarse para iterar sobre el conjunto de resultados del
     comando o consulta.
    </simpara>
    <simpara>
     Dado que <classname>MongoDB\Driver\Cursor</classname> implementa la
     interfaz <interfacename>Traversable</interfacename>, puede simplemente
     iterar sobre el conjunto de resultados con
     <link linkend="control-structures.foreach"><literal>foreach</literal></link>.
    </simpara>
   <programlisting role="php">
<![CDATA[
<?php

$manager = new MongoDB\Driver\Manager();

/* Insertar algunos documentos para que nuestra consulta devuelva información */
$bulkWrite = new MongoDB\Driver\BulkWrite;
$bulkWrite->insert(['name' => 'Ceres', 'size' => 946, 'distance' => 2.766]);
$bulkWrite->insert(['name' => 'Vesta', 'size' => 525, 'distance' => 2.362]);
$manager->executeBulkWrite("test.asteroids", $bulkWrite);

/* Consultar todos los elementos de la colección */
$query = new MongoDB\Driver\Query( [] );

/* Consultar la colección "asteroids" de la base de datos "test" */
$cursor = $manager->executeQuery("test.asteroids", $query);

/* $cursor ahora contiene un objeto que envuelve el conjunto de resultados. Usar
 * foreach() para iterar sobre todos los resultados */
foreach($cursor as $document) {
    print_r($document);
}

?>
]]>
   </programlisting>
   &example.outputs.similar;
   <screen>
<![CDATA[
stdClass Object
(
    [_id] => MongoDB\BSON\ObjectId Object
        (
            [oid] => 5a4cff2f122d3321565d8cc2
        )

    [name] => Ceres
    [size] => 946
    [distance] => 2.766
)
stdClass Object
(
    [_id] => MongoDB\BSON\ObjectId Object
        (
            [oid] => 5a4cff2f122d3321565d8cc3
        )

    [name] => Vesta
    [size] => 525
    [distance] => 2.362
}
]]>
    </screen>
   </example>

   <example xml:id="mongodb-driver-cursor.examples.tailable">
    <title>Lectura de un conjunto de resultados para un cursor rastreable</title>
    <simpara>
     Los <link xlink:href="&url.mongodb.docs;core/tailable-cursors">cursores rastreables</link>
     son un tipo especial de cursor de MongoDB que permite al cliente leer algunos
     resultados y luego esperar hasta que haya más documentos disponibles. Estos
     cursores se usan principalmente con
     <link xlink:href="&url.mongodb.docs;core/capped-collections">colecciones limitadas</link>
     y <link xlink:href="&url.mongodb.docs;changeStreams">flujos de cambios</link>.
    </simpara>
    <simpara>
     Mientras que los cursores normales pueden iterarse una vez con
     <literal>foreach</literal>, ese enfoque no funcionará con cursores rastreables.
     Cuando se usa <literal>foreach</literal> con un cursor rastreable, el bucle
     terminará al llegar al final del conjunto de resultados inicial. Intentar
     continuar la iteración en el cursor con un segundo
     <literal>foreach</literal> lanzaría una excepción, ya que PHP intenta
     retroceder el cursor. Al igual que los objetos de resultado en otros controladores
     de bases de datos, los cursores en MongoDB solo admiten la iteración hacia
     adelante, lo que significa que no pueden retrocederse.
    </simpara>
    <simpara>
     Para leer de forma continua desde un cursor rastreable, el objeto Cursor
     debe envolverse con un <classname>IteratorIterator</classname>. Esto permite
     que la aplicación controle directamente la iteración del cursor, evite
     retroceder el cursor sin querer y decida cuándo esperar nuevos resultados
     o detener completamente la iteración.
    </simpara>
    <simpara>
     Para demostrar un cursor rastreable en acción, se usarán dos scripts: un
     "productor" y un "consumidor". El script productor creará una nueva colección
     limitada usando el comando
     <link xlink:href="&url.mongodb.docs.command;create">create</link> e insertará
     un nuevo documento en esa colección cada segundo.
    </simpara>
    <programlisting role="php">
<![CDATA[
<?php

$manager = new MongoDB\Driver\Manager;

$manager->executeCommand('test', new MongoDB\Driver\Command([
    'create' => 'asteroids',
    'capped' => true,
    'size' => 1048576,
]));

while (true) {
    $bulkWrite = new MongoDB\Driver\BulkWrite;
    $bulkWrite->insert(['createdAt' => new MongoDB\BSON\UTCDateTime]);
    $manager->executeBulkWrite('test.asteroids', $bulkWrite);

    sleep(1);
}

?>
]]>
    </programlisting>
    <simpara>
     Con el script productor aún en ejecución, puede ejecutarse un segundo script
     consumidor para leer los documentos insertados usando un cursor rastreable,
     indicado por las opciones <literal>tailable</literal> y
     <literal>awaitData</literal> en <function>MongoDB\Driver\Query::__construct</function>.
    </simpara>
    <programlisting role="php">
<![CDATA[
<?php

$manager = new MongoDB\Driver\Manager;

$query = new MongoDB\Driver\Query([], [
    'tailable' => true,
    'awaitData' => true,
]);

$cursor = $manager->executeQuery('test.asteroids', $query);

$iterator = new IteratorIterator($cursor);

$iterator->rewind();

while (true) {
    if ($iterator->valid()) {
        $document = $iterator->current();
        printf("Documento consumido creado en: %s\n", $document->createdAt);
    }

    $iterator->next();
}

?>
]]>
    </programlisting>
    <simpara>
     El script consumidor comenzará imprimiendo rápidamente todos los documentos
     disponibles en la colección limitada (como si se hubiera usado
     <literal>foreach</literal>); sin embargo, no terminará al llegar al final
     del conjunto de resultados inicial. Dado que el cursor es rastreable, llamar
     a <function>IteratorIterator::next</function> bloqueará y esperará
     resultados adicionales. <function>IteratorIterator::valid</function> también
     se usa para verificar si hay realmente datos disponibles para leer en cada
     paso.
    </simpara>
    <note>
     <simpara>
      Este ejemplo usa la opción de consulta <literal>awaitData</literal> para
      instruir al servidor que bloquee durante un breve período (por ejemplo,
      un segundo) al final del conjunto de resultados antes de devolver una
      respuesta al controlador. Esto se usa para evitar que el controlador
      consulte agresivamente al servidor cuando no hay resultados disponibles.
      La opción <literal>maxAwaitTimeMS</literal> puede usarse junto con
      <literal>tailable</literal> y <literal>awaitData</literal> para especificar
      la cantidad de tiempo que el servidor debe bloquearse cuando llega al final
      del conjunto de resultados.
     </simpara>
    </note>
   </example>
  </section>

  <section role="errors">
   &reftitle.errors;
   <simpara>
    Al iterar sobre el objeto cursor, los datos BSON se convierten en variables
    PHP. Esta iteración puede causar las siguientes Excepciones:
   </simpara>
   <simplelist>
    <member>
     Lanza
     <classname>MongoDB\Driver\Exception\InvalidArgumentException</classname> si
     una clase en el mapa de tipos no puede ser instanciada o no implementa
     <interfacename>MongoDB\BSON\Unserializable</interfacename>.
    </member>
    &mongodb.throws.bson.unexpected;
   </simplelist>
  </section>

 </partintro>

 &reference.mongodb.mongodb.driver.entities.cursor;

</reference>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
