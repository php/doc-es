<?xml version="1.0" encoding="utf-8"?>
<!-- EN-Revision: 9f4cb232d01a06077a2324e38f767d63f87f2e5f Maintainer: PhilDaiguille Status: ready -->
<!-- Reviewed: no -->
<section xml:id="mongodb.installation" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
 &reftitle.install;

 <section xml:id="mongodb.installation.pie">
  <title>Instalación de la extensión MongoDB PHP con PIE</title>

   &pecl.moving.to.pie;

   <para>
    Para instalar la extensión MongoDB usando PIE, ejecute el siguiente comando:
    <programlisting role="shell">
<![CDATA[
pie install mongodb/mongodb-extension
]]>
    </programlisting>
   </para>

 </section>

 <section xml:id="mongodb.installation.pecl">
  <title>Instalación de la extensión MongoDB PHP con PECL</title>

  <simpara>
   &pecl.info;
   <link xlink:href="&url.pecl.package;mongodb">&url.pecl.package;mongodb</link>
  </simpara>

  <para>
   Los usuarios de Linux, Unix y macOS pueden ejecutar el siguiente comando para instalar la
   extensión:
   <programlisting role="shell">
<![CDATA[
$ sudo pecl install mongodb
]]>
   </programlisting>
  </para>

  <simpara>
   En sistemas con múltiples versiones de PHP instaladas (por ejemplo, la versión predeterminada de macOS,
   Homebrew, <link xlink:href="&url.xampp;">XAMPP</link>), cada versión de PHP
   tendrá su propio comando <link linkend="install.pecl">pecl</link>
   y archivo(s) &php.ini;. Además, cada entorno de PHP (por ejemplo,
   CLI, web) puede usar archivos &php.ini; separados.
  </simpara>

  <para>
   A partir de la versión 1.17.0 de la extensión, PECL solicitará varias
   opciones de <literal>configure</literal>. Para instalar la extensión con las
   opciones predeterminadas en un script no interactivo, se puede enviar una entrada de cadena vacía
   a <literal>pecl install</literal> usando el comando <literal>yes</literal>:
   <programlisting role="shell">
<![CDATA[
$ yes '' | sudo pecl install mongodb
]]>
   </programlisting>
  </para>

  <para>
   Se puede encontrar una lista completa de las opciones de <literal>configure</literal> admitidas
   en el archivo <literal>package.xml</literal> incluido en el paquete PECL.
   Para instalar la extensión con opciones específicas de <literal>configure</literal> en un script
   no interactivo, se puede usar la opción <literal>--configureoptions</literal> para
   <literal>pecl install</literal>:
   <programlisting role="shell">
<![CDATA[
$ sudo pecl install --configureoptions='with-mongodb-system-libs="yes" enable-mongodb-developer-flags="no"' mongodb
]]>
   </programlisting>
  </para>

  <simpara>
   Por omisión, instalar la extensión mediante PECL usará versiones integradas de
   <link xlink:href="&url.mongodb.libbson;">libbson</link>,
   <link xlink:href="&url.mongodb.libmongoc;">libmongoc</link> y
   <link xlink:href="&url.mongodb.libmongocrypt;">libmongocrypt</link> e intentará
   configurarlas automáticamente.
  </simpara>

  <note>
   <simpara>
    Si el proceso de compilación no logra encontrar una biblioteca SSL, verifique que los paquetes
    de desarrollo (por ejemplo, <literal>libssl-dev</literal>) y
    <link xlink:href="&url.mongodb.wiki.pkg-config;">pkg-config</link> estén ambos
    instalados. Si esto no resuelve el problema, considere usar el
    <link linkend="mongodb.installation.manual">proceso de instalación manual</link>.
   </simpara>
  </note>

  <para>
   Finalmente, agregue la siguiente línea al archivo &php.ini; para cada entorno
   que necesite usar la extensión:
   <programlisting role="ini">
<![CDATA[
extension=mongodb.so
]]>
   </programlisting>
  </para>
 </section>

 <section xml:id="mongodb.installation.homebrew">
  <title>Instalación de la extensión MongoDB PHP en macOS con Homebrew</title>

  <simpara>
   <link xlink:href="&url.brew.1.5.0;">Homebrew 1.5.0</link>
   dejó de dar soporte al <link xlink:href="&url.mac.homebrew;">Homebrew/php tap</link>
   y eliminó las fórmulas para extensiones individuales de PHP. A partir de ahora, los usuarios de macOS
   deben instalar la fórmula
   <link xlink:href="&url.brew.php;">php</link>
   y seguir las instrucciones estándar de
   <link linkend="mongodb.installation.pecl">instalación con PECL</link>
   usando el comando <link linkend="install.pecl">pecl</link> proporcionado por la
   instalación de PHP de Homebrew.
  </simpara>
  <para>
   Alternativamente, el
   <link xlink:href="https://github.com/shivammathur/homebrew-extensions">repositorio shivammathur/extensions</link>
   proporciona fórmulas para extensiones individuales de PHP. Por ejemplo, para instalar
   la extensión para PHP 8.4, ejecute:
   <programlisting role="shell">
<![CDATA[
$ brew install shivammathur/extensions/mongodb@8.4
]]>
   </programlisting>
   Tenga en cuenta que solo está disponible la versión más reciente de la extensión en brew.
  </para>
  <note>
   <title>Instalar las dependencias requeridas</title>
   <simpara>
    Para garantizar que el soporte SSL pueda configurarse correctamente, asegúrese de que las fórmulas
    <literal>openssl</literal> y <literal>pkgconf</literal> estén
    instaladas. Si falta alguno de estos paquetes, la extensión se compilará con Secure Transport,
    lo que puede generar problemas de compatibilidad.
   </simpara>
  </note>
 </section>

 <section xml:id="mongodb.installation.windows">
  <title>Instalación de la extensión MongoDB PHP en Windows</title>

  <simpara>
   Se adjuntan binarios precompilados a los
   <link xlink:href="&url.mongodb.github.new;/releases/">lanzamientos del proyecto en Github</link>.
   Se publican archivos para varias combinaciones de versión de PHP, seguridad de subprocesos
   (TS o NTS) y arquitectura (x86 o x64). Determine el archivo correcto para el entorno de PHP y
   extraiga el archivo <filename>php_mongodb.dll</filename> al directorio de extensiones
   ("ext" por omisión).
  </simpara>

  <para>
   Agregue la siguiente línea al archivo &php.ini; para cada entorno que necesite
   usar la extensión:
   <programlisting role="ini">
<![CDATA[
extension=php_mongodb.dll
]]>
   </programlisting>
  </para>

  <para>
   Si no selecciona el binario correcto, se producirá un error al intentar cargar la DLL de la extensión
   en tiempo de ejecución:
   <programlisting role="txt">
<![CDATA[
PHP Warning:  PHP Startup: Unable to load dynamic library 'mongodb'
]]>
   </programlisting>
  </para>

  <para>
   Asegúrese de que la DLL descargada corresponda a las siguientes propiedades del entorno de ejecución de PHP:
   <simplelist>
    <member>Versión de PHP (<constant>PHP_VERSION</constant>)</member>
    <member>Seguridad de subprocesos (<constant>PHP_ZTS</constant>)</member>
    <member>Arquitectura (<constant>PHP_INT_SIZE</constant>)</member>
   </simplelist>
  </para>

  <simpara>
   Además de las constantes mencionadas anteriormente, estas propiedades también pueden inferirse
   desde la función <function>phpinfo</function>. Si un sistema tiene múltiples entornos de ejecución
   de PHP instalados, verifique que la salida de <function>phpinfo</function> corresponda al entorno correcto.
  </simpara>

  <note>
   <title>Dependencias adicionales de DLL para usuarios de Windows</title>
   <simpara>
    &ext.windows.path.dll;
    <filename>libsasl.dll</filename>
   </simpara>
  </note>
 </section>

 <section xml:id="mongodb.installation.manual">
  <title>Compilación del controlador MongoDB PHP desde el código fuente</title>

  <para>
   Para desarrolladores y usuarios interesados en las correcciones más recientes de errores, la extensión
   puede compilarse desde el código fuente más reciente en
   <link xlink:href="&url.mongodb.github.new;">Github</link>. Ejecute los siguientes
   comandos para clonar y compilar el proyecto:
   <programlisting role="shell">
<![CDATA[
$ git clone https://github.com/mongodb/mongo-php-driver.git
$ cd mongo-php-driver
$ git submodule update --init
$ phpize
$ ./configure
$ make all
$ sudo make install
]]>
   </programlisting>
  </para>

  <simpara>
   En sistemas con múltiples versiones de PHP instaladas (por ejemplo, la versión predeterminada de macOS,
   Homebrew, <link xlink:href="&url.xampp;">XAMPP</link>), cada versión de PHP
   tendrá su propio comando <link linkend="install.pecl.phpize">phpize</link>
   y archivo(s) &php.ini;. Además, cada entorno de PHP (por ejemplo,
   CLI, web) puede usar archivos &php.ini; separados.
  </simpara>

  <simpara>
   Por omisión, la extensión usará versiones integradas de
   <link xlink:href="&url.mongodb.libbson;">libbson</link>,
   <link xlink:href="&url.mongodb.libmongoc;">libmongoc</link> y
   <link xlink:href="&url.mongodb.libmongocrypt;">libmongocrypt</link> e intentará
   configurarlas automáticamente. Si estas bibliotecas ya están instaladas como bibliotecas del sistema,
   la extensión puede utilizarlas especificando <literal>--with-mongodb-system-libs=yes</literal> como
   opción para <literal>configure</literal>.
  </simpara>

  <simpara>
   Para obtener una lista completa de las opciones de <literal>configure</literal>, ejecute
   <command>configure --help</command>.
  </simpara>

  <simpara>
   Al usar versiones integradas de libmongoc y libmongocrypt, la extensión también intentará
   seleccionar una biblioteca SSL según la opción <literal>--with-mongodb-ssl</literal> de
   <literal>configure</literal>. A partir de la versión 1.17.0 de la extensión, OpenSSL siempre
   se prefiere por omisión. Anteriormente, Secure Transport era la opción predeterminada en macOS
   y OpenSSL era la opción predeterminada en todas las demás plataformas.
  </simpara>

  <note>
   <simpara>
    Si el proceso de compilación no logra encontrar una biblioteca SSL, verifique que los paquetes
    de desarrollo (por ejemplo, <literal>libssl-dev</literal>) y
    <link xlink:href="&url.mongodb.wiki.pkg-config;">pkg-config</link> estén ambos
    instalados.
   </simpara>

   <simpara>
    Al usar Homebrew en macOS, es común que un sistema tenga instaladas múltiples
    versiones de OpenSSL. Para garantizar que se seleccione la versión deseada de OpenSSL,
    se puede usar la variable de entorno <literal>PKG_CONFIG_PATH</literal> para controlar
    la ruta de búsqueda de <literal>pkg-config</literal>.
   </simpara>
  </note>

  <para>
   El paso final de compilación, <command>make install</command>, informará dónde
   se ha instalado <filename>mongodb.so</filename>, similar a:
   <programlisting role="txt">
<![CDATA[
Installing shared extensions:     /usr/lib/php/extensions/debug-non-zts-20220829/
]]>
   </programlisting>
  </para>

  <para>
   Asegúrese de que la opción <link linkend="ini.extension-dir">extension_dir</link>
   en &php.ini; apunte al directorio donde se instaló <filename>mongodb.so</filename>.
   La opción puede consultarse ejecutando:
   <programlisting role="shell">
<![CDATA[
$ php -i | grep extension_dir
  extension_dir => /usr/lib/php/extensions/debug-non-zts-20220829 =>
                   /usr/lib/php/extensions/debug-non-zts-20220829
]]>
   </programlisting>
  </para>

  <simpara>
   Si los directorios difieren, cambie
   <link linkend="ini.extension-dir">extension_dir</link> en &php.ini; o
   mueva manualmente <filename>mongodb.so</filename> al directorio correcto.
  </simpara>

  <para>
   Finalmente, agregue la siguiente línea al archivo &php.ini; para cada entorno
   que necesite usar la extensión:
   <programlisting role="ini">
<![CDATA[
extension=mongodb.so
]]>
   </programlisting>
  </para>
 </section>

</section>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
