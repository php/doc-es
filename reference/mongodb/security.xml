<?xml version="1.0" encoding="utf-8"?>
<!-- EN-Revision: 9f4cb232d01a06077a2324e38f767d63f87f2e5f Maintainer: PhilDaiguille Status: ready -->
<!-- Reviewed: no -->
<chapter xml:id="mongodb.security" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
 <title>Seguridad</title>

 <section xml:id="mongodb.security.request_injection">
  <title>Ataques por inyección de peticiones</title>
  <simpara>
   Si está pasando parámetros de <literal>$_GET</literal> (o <literal>$_POST</literal>)
   a sus consultas, asegúrese de que primero se conviertan a strings. Los usuarios
   pueden insertar arrays asociativos en peticiones GET y POST, lo que podría
   convertirse en consultas no deseadas con operadores $-.
  </simpara>

  <simpara>
   Un ejemplo bastante inocuo: supongamos que está buscando la información de un
   usuario con la petición <emphasis>http://www.example.com?username=bob</emphasis>.
   Su aplicación crea la consulta
   <literal>$q = new \MongoDB\Driver\Query( [ 'username' => $_GET['username'] ])</literal>.
  </simpara>

  <simpara>
   Alguien podría subvertir esto mediante
   <emphasis>http://www.example.com?username[$ne]=foo</emphasis>, que PHP
   convertirá mágicamente en un array asociativo, convirtiendo su consulta en
   <literal>$q = new \MongoDB\Driver\Query( [ 'username' => [ '$ne' => 'foo' ] ] )</literal>,
   lo que devolverá todos los usuarios que no se llamen "foo" (probablemente todos sus usuarios).
  </simpara>

  <simpara>
   Este es un ataque bastante fácil de defender: asegúrese de que los parámetros
   de $_GET y $_POST sean del tipo que espera antes de enviarlos a la base de datos.
   PHP tiene la función <function>filter_var</function> para ayudar con esto.
  </simpara>

  <simpara>
   Tenga en cuenta que este tipo de ataque puede usarse con cualquier interacción
   con la base de datos que localice un documento, incluyendo actualizaciones,
   upserts, eliminaciones y comandos findAndModify.
  </simpara>

  <simpara>
   Consulte <link xlink:href="&url.mongodb.dochub.security;">la documentación principal</link>
   para obtener más información sobre problemas similares a la inyección SQL con MongoDB.
  </simpara>
 </section>

 <section xml:id="mongodb.security.script_injection">
  <title>Ataques por inyección de scripts</title>
  <simpara>
   Si está usando JavaScript, asegúrese de que cualquier variable que cruce el
   límite entre PHP y JavaScript se pase en el campo <literal>scope</literal>
   de <classname>MongoDB\BSON\Javascript</classname>, no interpolada en la cadena
   de JavaScript. Esto puede ocurrir al usar cláusulas <literal>$where</literal>
   en consultas, comandos mapReduce y group, y cualquier otro momento en que pase
   JavaScript a la base de datos.
  </simpara>
  <simpara>
   Por ejemplo, supongamos que tenemos algo de JavaScript para saludar a un usuario
   en los registros de la base de datos. Podríamos hacer:
  </simpara>
  <programlisting role="php">
<![CDATA[
<?php
$m = new MongoDB\Driver\Manager;

// ¡No haga esto!
$username = $_GET['field'];

$cmd = new \MongoDB\Driver\Command( [
    'eval' => "print('Hola, $username!');"
] );

$r = $m->executeCommand( 'dramio', $cmd );
?>
]]>
  </programlisting>
  <simpara>
   Sin embargo, ¿qué pasaría si un usuario malicioso pasa algo de JavaScript?
  </simpara>
  <programlisting role="php">
<![CDATA[
<?php
$m = new MongoDB\Driver\Manager;

// ¡No haga esto!
$username = $_GET['field'];
// $username se establece en "'); db.users.drop(); print('"

$cmd = new \MongoDB\Driver\Command( [
    'eval' => "print('Hola, $username!');"
] );

$r = $m->executeCommand( 'dramio', $cmd );
?>
]]>
  </programlisting>
  <simpara>
   Ahora MongoDB ejecuta la cadena de JavaScript
   <literal>"print('Hola, '); db.users.drop(); print('!');"</literal>.
   Este ataque es fácil de evitar: use <literal>args</literal> para pasar
   variables de PHP a JavaScript:
  </simpara>
  <programlisting role="php">
<![CDATA[
<?php
$m = new MongoDB\Driver\Manager;

$_GET['field'] = 'derick';
$args = [ $_GET['field'] ];

$cmd = new \MongoDB\Driver\Command( [
    'eval' => "function greet(username) { print('Hola, ' + username + '!'); }",
    'args' => $args,
] );

$r = $m->executeCommand( 'dramio', $cmd );
?>
]]>
  </programlisting>
  <simpara>
   Esto añade un argumento al ámbito de JavaScript, que se usa como argumento
   para la función <literal>greet</literal>. Ahora si alguien intenta enviar
   código malicioso, MongoDB imprimirá inocuamente
   <literal>Hola, '); db.dropDatabase(); print('!</literal>.
  </simpara>

  <simpara>
   Usar argumentos ayuda a prevenir que la entrada maliciosa se ejecute en la
   base de datos. Sin embargo, debe asegurarse de que su código no vuelva a
   ejecutar la entrada de todos modos. Lo mejor es evitar ejecutar
   <emphasis>cualquier</emphasis> JavaScript en el servidor desde el principio.
  </simpara>

  <simpara>
   Se recomienda encarecidamente evitar el uso de la cláusula <link
   xlink:href="&url.mongodb.docs;reference/operator/query/where/#considerations"><literal>$where</literal></link>
   en consultas, ya que afecta significativamente al rendimiento. Siempre que sea
   posible, use operadores de consulta normales o el <link
   xlink:href="&url.mongodb.docs;core/aggregation-pipeline">Marco de
   Agregación</link>.
  </simpara>

  <simpara>
   Como alternativa al <link
   xlink:href="&url.mongodb.dochub.mapreduce;">MapReduce</link>, que usa
   JavaScript, considere usar el <link
   xlink:href="&url.mongodb.docs;core/aggregation-pipeline">Marco de
   Agregación</link>. A diferencia de Map/Reduce, usa un lenguaje idiomático
   para construir consultas, sin tener que escribir y usar el enfoque más lento
   de JavaScript que requiere Map/Reduce.
  </simpara>

  <simpara>
   El comando <link
   xlink:href="&url.mongodb.docs;reference/command/eval/">eval</link> ha sido
   deprecado desde MongoDB 3.0 y también debe evitarse.
  </simpara>
 </section>
</chapter>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
