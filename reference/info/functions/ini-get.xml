<?xml version="1.0" encoding="iso-8859-1"?>
<!-- $Revision: 1.5 $ -->
<!-- EN-Revision: 1.13 Maintainer: lboshell Status: ready -->
<!-- splitted from ./en/functions/info.xml, last change in rev 1.64 -->
  <refentry id="function.ini-get">
   <refnamediv>
    <refname>ini_get</refname>
    <refpurpose>Obtiene el valor de una opci&oacute;n de
    configuraci&oacute;n</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Descripci&oacute;n</title>
     <methodsynopsis>
      <type>string</type><methodname>ini_get</methodname>
      <methodparam><type>string</type><parameter>nombre_var</parameter></methodparam>
     </methodsynopsis>
    <para>
     Devuelve el valor de la opci&oacute;n de configuraci&oacute;n en
     caso de &eacute;xito. Si ocurre un fallo, como que se realice una
     consulta por un valor que no existe, devolver&aacute; una cadena
     vac&iacute;a.
    </para>
    <note>
     <title>Cuando se consultan valores booleanos</title>
     <para>
      Un valor ini booleano de <literal>off</literal> ser&aacute;
      devuelto como una cadena vac&iacute;a o "0", mientras que un valor ini
      booleano de <literal>on</literal> ser&aacute; devuelto como "1".
     </para>
    </note>
    <note>
     <title>Cuando se consultan valores de tama&ntilde;o de memoria</title>
     <para>
      Muchos valores ini de tama&ntilde;o de memoria, como <link
      linkend="ini.upload-max-filesize">upload_max_filesize</link> son
      almacenados en el archivo &php.ini; en notaci&oacute;n
      abreviada. <function>ini_get</function> devolver&aacute; la
      cadena exacta almacenada en el archivo &php.ini; y
      <emphasis>NO</emphasis> su equivalente
      <type>integer</type>. Usar funciones aritm&eacute;ticas normales
      sobre &eacute;stos valores no tendr&iacute;an los resultados que
      de otra forma podr&iacute;an esperarse. El siguiente ejemplo
      muestra una manera de convertir la notaci&oacute;n corta de
      bytes, de forma similar al modo que lo hace el c&oacute;digo
      fuente de PHP.
     </para>
    </note>
    <para>
     <example>
      <title>Algunos ejemplos de <function>ini_get</function></title>
       <programlisting role="php">
<![CDATA[
<?php
/*
Nuestro php.ini contiene los siguientes parametros:

display_errors = On
register_globals = Off
post_max_size = 8M
*/

echo 'display_errors = ' . ini_get('display_errors') . "\n";
echo 'register_globals = ' . ini_get('register_globals') . "\n";
echo 'post_max_size = ' . ini_get('post_max_size') . "\n";
echo 'post_max_size+1 = ' . (ini_get('post_max_size')+1) . "\n";
echo 'post_max_size en bytes = ' . return_bytes(ini_get('post_max_size'));

function return_bytes($val) {
    $val = trim($val);
    $ultimo = strtolower($val{strlen($val)-1});
    switch($ultimo) {
        // El modificador 'G' se encuentra disponible desde PHP 5.1.0
        case 'g':
            $val *= 1024;
        case 'm':
            $val *= 1024;
        case 'k':
            $val *= 1024;
    }

    return $val;
}

?>
]]>
       </programlisting>
       <para>
        Este script producir&aacute;:
       </para>
       <screen>
<![CDATA[

display_errors = 1
register_globals = 0
post_max_size = 8M
post_max_size+1 = 9
post_max_size en bytes = 8388608

]]>
       </screen>
      </example>
     </para>
    <para>
     Vea tambi&eacute;n <function>get_cfg_var</function>,
     <function>ini_get_all</function>,
     <function>ini_restore</function>, y <function>ini_set</function>.
    </para>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
