<?xml version="1.0" encoding="utf-8"?>
<!-- EN-Revision: 1340d3595bde8489ea1385868ffd75471a56999b Maintainer: PhilDaiguille Status: ready -->
<!-- Reviewed: no -->
<chapter xml:id="tutorial" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
  <info><title>Un tutorial sencillo</title></info>

  <para>
   Aquí nos gustaría mostrar los aspectos más básicos de PHP en un tutorial breve y sencillo.
   Este texto solo trata sobre la creación de páginas web dinámicas con PHP, aunque PHP no
   solo es capaz de crear páginas web. Consulte la sección titulada <link linkend="intro-whatcando">¿Qué
   puede hacer PHP</link> para obtener más información.
  </para>
  <para>
   Las páginas web habilitadas para PHP se tratan exactamente igual que las páginas HTML
   regulares y puede crearlas y editarlas de la misma manera que normalmente crea
   páginas HTML regulares.
  </para>

  <section xml:id="tutorial.firstpage">
   <info><title>Su primera página habilitada para PHP</title></info>
   <simpara>
    Este tutorial asume que PHP ya está instalado.
    Las instrucciones de instalación se pueden encontrar en la
    <link xlink:href="&url.php.downloads;">página de descargas</link>.
   </simpara>
   <para>
    Cree un fichero llamado <filename>hello.php</filename>
    con el siguiente contenido:
   </para>
   <para>
    <example>
     <info><title>Nuestro primer script PHP: <filename>hello.php</filename></title></info>
     <programlisting role="php">
<![CDATA[
<?php

echo "Hello World!";

?>
]]>
     </programlisting>
     <simpara>
      Usando su terminal, navegue hasta el directorio que contiene este fichero y
      inicie un servidor de desarrollo con el siguiente comando:
     </simpara>
     <programlisting role="shell">
<![CDATA[
php -S localhost:8000
]]>
     </programlisting>
     <simpara>
      Utilice su navegador para acceder al fichero con la URL de su servidor web,
      terminando con la referencia al fichero <literal>/hello.php</literal>.
      Según el comando anterior ejecutado, la URL será
      <literal>http://localhost:8000/hello.php</literal>.
      Si todo está configurado correctamente, este fichero será analizado por PHP
      y verá la salida "Hello World!" mostrada en su navegador.
     </simpara>
     <simpara>
      PHP puede incrustarse dentro de una página web HTML normal. Esto significa que
      dentro de su documento HTML puede escribir las sentencias PHP, como se
      demuestra en el siguiente ejemplo:
     </simpara>
     <programlisting role="php">
<![CDATA[
<!DOCTYPE html>
<html>
    <head>
        <title>Prueba de PHP</title>
    </head>
    <body>
        <?php echo '<p>Hello World</p>'; ?>
    </body>
</html>
]]>
     </programlisting>
     <simpara>
      Esto producirá la siguiente salida:
     </simpara>
     <screen role="html">
<![CDATA[
<!DOCTYPE html>
<html>
    <head>
        <title>Prueba de PHP</title>
    </head>
    <body>
        <p>Hello World</p>
    </body>
</html>
]]>
     </screen>
    </example>
   </para>
   <para>
    Este programa es extremadamente simple y realmente no necesitaba usar
    PHP para crear una página como esta. Todo lo que hace es mostrar:
    <literal>Hello World</literal> usando la sentencia <function>echo</function>
    de PHP. Observe que el fichero <emphasis>no necesita ser ejecutable</emphasis>
    ni especial de ninguna manera. El servidor descubre que este fichero necesita
    ser interpretado por PHP porque usó la extensión ".php", que el servidor está
    configurado para pasar a PHP. Piense en esto como un fichero HTML normal que
    por casualidad tiene un conjunto de etiquetas especiales disponibles que hacen
    muchas cosas interesantes.
   </para>

   <para>
    El objetivo del ejemplo es mostrar el formato especial de las etiquetas PHP.
    En este ejemplo usamos <literal>&lt;?php</literal> para indicar el
    inicio de una etiqueta PHP. Luego pusimos la sentencia PHP y salimos del
    modo PHP añadiendo la etiqueta de cierre, <literal>?&gt;</literal>.
    Puede entrar y salir del modo PHP en un fichero HTML como este donde
    quiera. Para más detalles, lea la sección del manual sobre la
    <link linkend="language.basic-syntax">sintaxis básica de PHP</link>.
   </para>

   <note>
    <info><title>Una nota sobre los saltos de línea</title></info>
    <para>
     Los saltos de línea tienen poco significado en HTML, sin embargo sigue siendo
     una buena idea hacer que su HTML se vea bien y limpio poniendo saltos de línea.
     Un salto de línea que sigue inmediatamente después de un cierre
     <literal>?&gt;</literal> será eliminado por PHP. Esto puede ser
     extremadamente útil cuando está poniendo muchos bloques de PHP o ficheros
     incluidos que contienen PHP y que no deberían mostrar nada. Al mismo tiempo
     puede ser un poco confuso. Puede poner un espacio después del cierre
     <literal>?&gt;</literal> para forzar que se muestre un espacio y un salto
     de línea, o puede poner un salto de línea explícito en el último echo/print
     desde dentro de su bloque PHP.
    </para>
   </note>

   <note>
    <info><title>Una nota sobre los editores de texto</title></info>
    <para>
     Hay muchos editores de texto y Entornos de Desarrollo Integrados (IDE) que
     puede usar para crear, editar y gestionar ficheros PHP. Una lista parcial de
     estas herramientas se mantiene en <link xlink:href="&url.phpeditorlist;">Lista de
     editores PHP</link>. Si desea recomendar un editor, por favor visite la
     página anterior y pídale al responsable de la página que añada el editor a
     la lista. Tener un editor con resaltado de sintaxis puede ser útil.
    </para>
   </note>

   <note>
    <info><title>Una nota sobre los procesadores de texto</title></info>
    <para>
     Los procesadores de texto como StarOffice Writer, Microsoft Word y Abiword no
     son óptimos para editar ficheros PHP. Si desea usar uno para este script de
     prueba, debe asegurarse de guardar el fichero como <emphasis>texto plano</emphasis>
     o PHP no podrá leer y ejecutar el script.
    </para>
   </note>

   <para>
    Ahora que ha creado con éxito un script PHP funcional, es hora de crear
    ¡el script PHP más famoso! Haga una llamada a la función
    <function>phpinfo</function> y verá mucha información útil sobre su
    sistema y configuración, como las <link linkend="language.variables.predefined">variables
    predefinidas</link> disponibles, los módulos PHP cargados y las
    <link linkend="configuration">configuraciones</link>. Tómese su tiempo y revise
    esta información importante.
   </para>
   <para>
    <example>
     <info><title>Obtener información del sistema desde PHP</title></info>
     <programlisting role="php">
<![CDATA[
<?php

phpinfo();

?>
]]>
     </programlisting>
    </example>
   </para>
  </section>

  <section xml:id="tutorial.useful">
   <info><title>Algo útil</title></info>
   <para>
    Ahora hagamos algo más útil. Vamos a comprobar qué tipo de navegador
    está usando el visitante. Para ello, comprobamos la cadena del agente de
    usuario que el navegador envía como parte de la petición HTTP. Esta
    información se almacena en una <link linkend="language.variables">variable</link>.
    Las variables siempre comienzan con un signo de dólar en PHP. La variable que
    nos interesa ahora es <varname>$_SERVER['HTTP_USER_AGENT']</varname>.
   </para>
   <note>
    <para>
     <varname>$_SERVER</varname> es una variable especial reservada de PHP que
     contiene toda la información del servidor web. Se la conoce como superglobal.
     Consulte la página relacionada del manual sobre
     <link linkend="language.variables.superglobals">superglobals</link> para
     obtener más información.
    </para>
   </note>
   <para>
    Para mostrar esta variable, puede hacer simplemente:
   </para>
   <para>
    <example>
     <info><title>Mostrar una variable (elemento de array)</title></info>
    <programlisting role="php">
<![CDATA[
<?php

echo $_SERVER['HTTP_USER_AGENT'];

?>
]]>
    </programlisting>
    <para>
     Una salida de ejemplo de este script podría ser:
    </para>
    <screen role="html">
<![CDATA[
Mozilla/5.0 (Linux) Firefox/112.0
]]>
    </screen>
   </example>
   </para>
   <para>
    Hay muchos <link linkend="language.types">tipos</link> de variables
    disponibles en PHP. En el ejemplo anterior imprimimos un elemento de un
    <link linkend="language.types.array">array</link> variable. Los arrays pueden
    ser muy útiles.
   </para>
   <para>
    <varname>$_SERVER</varname> es solo una variable que PHP hace
    automáticamente disponible. Puede ver una lista en la sección
    <link linkend="reserved.variables">Variables Reservadas</link> del manual
    o puede obtener una lista completa de ellas mirando la salida de la
    función <function>phpinfo</function> usada en el ejemplo de la sección
    anterior.
   </para>
   <para>
    Puede poner múltiples sentencias PHP dentro de una etiqueta PHP y crear
    pequeños bloques de código que hacen más que un simple echo. Por ejemplo,
    si quiere comprobar si se está usando Firefox puede hacer esto:
   </para>
   <para>
    <example>
     <info><title>Ejemplo usando <link linkend="language.control-structures">estructuras
     de control</link> y <link linkend="language.functions">funciones</link></title></info>
     <programlisting role="php">
<![CDATA[
<?php

if (str_contains($_SERVER['HTTP_USER_AGENT'], 'Firefox')) {
    echo 'Está usando Firefox.';
}

?>
]]>
     </programlisting>
     <para>
      Una salida de ejemplo de este script podría ser:
     </para>
     <screen role="html">
<![CDATA[
Está usando Firefox.
]]>
     </screen>
    </example>
   </para>
   <para>
    Aquí introducimos un par de conceptos nuevos. Tenemos una
    sentencia <link linkend="control-structures.if">if</link>.
    Si está familiarizado con la sintaxis básica usada por el lenguaje C,
    esto debería parecerle lógico. De lo contrario, debería leer un libro
    introductorio de PHP o los primeros capítulos, o leer la parte de
    <link linkend="langref">Referencia del Lenguaje</link> del manual.
   </para>
   <para>
    El segundo concepto que introdujimos fue la llamada a la función
    <function>str_contains</function>. <function>str_contains</function> es una
    función integrada en PHP que determina si una cadena dada contiene otra
    cadena. En este caso estamos buscando <literal>'Firefox'</literal> (la
    llamada aguja) dentro de <varname>$_SERVER['HTTP_USER_AGENT']</varname>
    (la llamada pajar). Si la aguja se encuentra dentro del pajar, la función
    devuelve true. De lo contrario, devuelve &false;. Si devuelve &true;, la
    expresión <link linkend="control-structures.if">if</link> se evalúa como
    &true; y se ejecuta el código dentro de sus {llaves}. De lo contrario, el
    código no se ejecuta. Siéntase libre de crear ejemplos similares, con
    <link linkend="control-structures.if">if</link>,
    <link linkend="control-structures.else">else</link>, y otras funciones
    como <function>strtoupper</function> y <function>strlen</function>.
    Cada página relacionada del manual contiene ejemplos también. Si no está
    seguro de cómo usar funciones, querrá leer tanto la página del manual sobre
    <link linkend="about.prototypes">cómo leer una definición de función</link> como
    la sección sobre <link linkend="language.functions">funciones de PHP</link>.
   </para>
   <para>
    Podemos llevar esto un paso más allá y mostrar cómo puede entrar y salir
    del modo PHP incluso en medio de un bloque PHP:
   </para>
   <para>
    <example>
     <info><title>Mezclar ambos modos HTML y PHP</title></info>
     <programlisting role="php">
<![CDATA[
<?php
if (str_contains($_SERVER['HTTP_USER_AGENT'], 'Firefox')) {
    ?>
    <h3>str_contains() devolvió true</h3>
    <p>Está usando Firefox</p>
    <?php
} else {
    ?>
    <h3>str_contains() devolvió false</h3>
    <p>No está usando Firefox</p>
    <?php
}
?>
]]>
     </programlisting>
     <para>
      Una salida de ejemplo de este script podría ser:
     </para>
     <screen role="html">
<![CDATA[
<h3>str_contains() devolvió true</h3>
<p>Está usando Firefox</p>
]]>
     </screen>
    </example>
   </para>
   <para>
    En lugar de usar una sentencia echo de PHP para mostrar algo, salimos
    del modo PHP y enviamos directamente HTML. El punto importante y potente
    a notar aquí es que el flujo lógico del script permanece intacto. Solo
    uno de los bloques HTML terminará siendo enviado al espectador dependiendo
    del resultado de <function>str_contains</function>. En otras palabras,
    depende de si la cadena <literal>Firefox</literal> fue encontrada o no.
   </para>
  </section>

  <section xml:id="tutorial.forms">
   <info><title>Tratar con formularios</title></info>
   <para>
    Una de las características más poderosas de PHP es la forma en que maneja
    los formularios HTML. El concepto básico que es importante entender es
    que cualquier elemento de formulario estará automáticamente disponible
    para sus scripts PHP. Por favor, lea la sección del manual sobre
    <link linkend="language.variables.external">Variables desde fuentes
    externas</link> para obtener más información y ejemplos sobre el uso
    de formularios con PHP. Aquí hay un ejemplo de formulario HTML:
   </para>
   <para>
    <example>
     <info><title>Un formulario HTML sencillo</title></info>
     <programlisting role="html">
<![CDATA[
<form action="action.php" method="post">
    <label for="name">Su nombre:</label>
    <input name="name" id="name" type="text">

    <label for="age">Su edad:</label>
    <input name="age" id="age" type="number">

    <button type="submit">Enviar</button>
</form>
]]>
     </programlisting>
    </example>
   </para>
   <para>
    No hay nada especial en este formulario. Es un formulario HTML directo
    sin ninguna etiqueta especial. Cuando el usuario rellena este formulario
    y pulsa el botón de enviar, se invoca la página <filename>action.php</filename>.
    En este fichero escribiría algo como esto:
   </para>
   <para>
    <example>
     <info><title>Mostrar datos de nuestro formulario</title></info>
     <programlisting role="php">
<![CDATA[
Hola <?php echo htmlspecialchars($_POST['name']); ?>.
Tiene <?php echo (int) $_POST['age']; ?> años.
]]>
     </programlisting>
     <para>
      Una salida de ejemplo de este script podría ser:
     </para>
     <screen role="html">
<![CDATA[
Hola Joe. Tiene 22 años.
]]>
     </screen>
    </example>
   </para>
   <para>
    Aparte de las partes <function>htmlspecialchars</function> y
    <literal>(int)</literal>, debería ser obvio lo que hace esto.
    <function>htmlspecialchars</function> asegura que cualquier carácter que
    sea especial en HTML se codifique correctamente para que la gente no pueda
    inyectar etiquetas HTML o Javascript en su página. Para el campo de edad,
    ya que sabemos que es un número, podemos simplemente
    <link linkend="language.types.typecasting">convertirlo</link> a un
    <type>int</type> que automáticamente eliminará cualquier carácter
    adicional. También puede hacer que PHP haga esto automáticamente por usted
    usando la <link linkend="ref.filter">extensión filter</link>.
    Las variables <varname>$_POST['name']</varname> y
    <varname>$_POST['age']</varname> son establecidas automáticamente por PHP.
    Anteriormente usamos la superglobal <varname>$_SERVER</varname>; arriba
    acabamos de introducir la superglobal <varname>$_POST</varname> que
    contiene todos los datos POST. Observe cómo el <emphasis>método</emphasis>
    de nuestro formulario es POST. Si hubiéramos usado el método
    <emphasis>GET</emphasis> entonces nuestra información del formulario viviría
    en la superglobal <varname>$_GET</varname> en su lugar.
    También puede usar la superglobal <varname>$_REQUEST</varname> si no le
    importa la fuente de sus datos de petición. Contiene la información
    combinada de los datos GET, POST y COOKIE.
   </para>
  </section>

  <section xml:id="tutorial.whatsnext">
   <info><title>¿Qué sigue?</title></info>
   <simpara>
    Con sus nuevos conocimientos debería ser capaz de entender la mayoría del manual.
   </simpara>
   <para>
    En particular puede que quiera explorar las siguientes características:
    <simplelist>
     <member>Leer y escribir ficheros con las <link linkend="book.filesystem">funciones del sistema de ficheros</link></member>
     <member><link linkend="features.file-upload">Manejo de subidas de ficheros</link></member>
     <member>Obtener páginas y ficheros remotos con <link linkend="book.curl">Curl</link></member>
     <member>
      Almacenar y analizar datos en una base de datos con <link linkend="book.pdo">PDO</link>
      (<link linkend="ref.pdo-sqlite">SQLite</link> puede usarse sin ejecutar un servidor de base de datos)
     </member>
     <member>Persistir datos entre peticiones con <link linkend="book.session">Sesiones</link></member>
    </simplelist>
   </para>
   <simpara>
    Hay un tesoro de bibliotecas y <link xlink:href="&url.packagist;/search/?tags=framework">marcos de referencia</link>
    para cada ocasión en el repositorio <link xlink:href="&url.packagist;">Packagist</link>, todos instalables
    mediante el <link linkend="install.composer.intro">gestor de paquetes Composer</link>.
   </simpara>
   <simpara>
    Para obtener ayuda y consejos de la comunidad, consulte la <link xlink:href="&url.php.support;">página de ayuda</link>.
   </simpara>
   <simpara>
    Para una variedad de podcasts, presentaciones y otros vídeos, consulte el
    <link xlink:href="&url.phpctv;">PeerTube de la comunidad</link>.
   </simpara>
   <simpara>
    Otros recursos de la comunidad que le ayudarán incluyen "listas increíbles"
    (directorios curados de enlaces) y "mapas de ruta para desarrolladores"
    (listas de temas relacionados).
   </simpara>
   <para>
    Cuando se quede atascado sin saber por dónde empezar, intente dividir su
    proyecto o problema en partes más pequeñas, lo que le permitirá ver más
    fácilmente qué ya sabe hacer y qué necesita aprender. La lista puede ser
    tan detallada como necesite. Por ejemplo, construir un blog podría
    desglosarse en las siguientes partes:
    <itemizedlist>
     <listitem>
      <simpara>Listar y ver páginas</simpara>
      <itemizedlist>
       <listitem>
        <simpara>Leer registros (páginas) de una base de datos</simpara>
       </listitem>
      </itemizedlist>
     </listitem>
     <listitem>
      <simpara>Crear páginas</simpara>
      <itemizedlist>
       <listitem>
        <simpara>Manejar el envío del formulario</simpara>
       </listitem>
       <listitem>
        <simpara>Escribir registros (páginas) en una base de datos</simpara>
       </listitem>
      </itemizedlist>
     </listitem>
     <listitem>
      <simpara>Acceso de administrador</simpara>
      <itemizedlist>
       <listitem>
        <simpara>Leer registros (usuarios) de una base de datos</simpara>
       </listitem>
       <listitem>
        <simpara>Manejar contraseñas</simpara>
       </listitem>
       <listitem>
        <simpara>Persistir datos (inicio de sesión de usuario) entre peticiones/páginas (sesiones)</simpara>
       </listitem>
      </itemizedlist>
     </listitem>
    </itemizedlist>
   </para>
   <simpara>
    Si no hay nada en particular que quiera construir, puede intentar buscar
    ejercicios de codificación como katas, desafíos y "code golf". Incluso
    cuando no están específicamente dirigidos a PHP, la mayoría deberían poder
    completarse y probablemente desafiarán sus conocimientos y pensamiento.
   </simpara>
  </section>
 </chapter>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
