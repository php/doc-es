 <reference id="ref.exec">
  <title>Funciones de ejecución de programas</title>
  <titleabbrev>Ejecución de programas</titleabbrev>

  <refentry id="function.escapeshellcmd">
   <refnamediv>
    <refname>escapeshellcmd</refname>
    <refpurpose>enmascara los metacaracteres del intérprete de ordenes
    </refpurpose>
   </refnamediv>
   <refsect1>
    <title>Descripción</title>
    <funcsynopsis>
     <funcdef>string <function>escapeshellcmd</function></funcdef>
     <paramdef>string <parameter>command</parameter></paramdef>
    </funcsynopsis>
    <para>
     <function>EscapeShellCmd</function> enmascara cualquier carácter en una
     cadena de caracteres que pueda usarse para introducir fraudulentamente
     una orden al intérprete de órdenes para que éste ejecute instrucciones
     arbitrarias. Esta función se debería usar para asegurarse que
     cualquier dato que venga del usuario se enmascare antes de que éste se le
     pase a las funciones <function>exec</function> o
     <function>system</function>, o al <link
     linkend="language.operators.execution">operador ` (apóstrofe invertido)
     </link>. Un uso habitual podría ser:</para>
    <para>
     <informalexample>
      <programlisting>
system(EscapeShellCmd($cmd))
      </programlisting>
     </informalexample>
	</para>

	<para>
	 Véase también <function>exec</function>, <function>popen</function>,
	 <function>system</function>, y el <link
	 linkend="language.operators.execution">operador ` (apóstrofe
         invertido)</link>.
	</para>

   </refsect1>
  </refentry>

  <refentry id="function.exec">
   <refnamediv>
    <refname>exec</refname>
    <refpurpose>Ejecuta un programa externo</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Descripción</title>
    <funcsynopsis>
     <funcdef>string <function>exec</function></funcdef>
     <paramdef>string <parameter>command</parameter></paramdef>
     <paramdef>string <parameter><optional>array</optional></parameter></paramdef>
     <paramdef>int <parameter><optional>return_var</optional></parameter></paramdef>
    </funcsynopsis>

    <para>
    <function>exec</function> ejecuta la orden indicada en
    <parameter>command</parameter>, sin embargo no produce ninguna
    salida. Simplemente devuelve la última línea de la salida resultado
    de la orden.  Si necesita ejecutar una orden y obtener directamente
    todos los datos devueltos por la orden sin ninguna interferencia,
    use la función <function>PassThru</function>.
    </para>
    <para>
    Si el parámetro <parameter>array</parameter> existe, entonces el
    array especificado se rellenará con cada una de las líneas de la
    salida producida por la orden. Notar que si el array ya contiene
    algunos elementos, <function>exec</function> los añadirá al final del
    array. Si no quiere que la función añada dichos elementos, haga un
    <function>unset</function> sobre el array antes de pasárselo a
    <function>exec</function>.</para>
    <para>
    Si el parámetro <parameter>return_var</parameter> existe a la vez
    que el parámetro <parameter>array</parameter>, entonces el valor de
    retorno de la orden ejecutada se guardará en dicha variable.
    </para>
    <para>
    Destacar que si usted va a permitir que se pasen datos provenientes
    de usuarios a esta función, entonces debería usar
    <function>EscapeShellCmd</function> para asegurarse de que los
    usuarios no pueden engañar al sistema para ejecutar instrucciones
    arbitrarias.
    </para>

    <para>
	 Véase también <function>system</function>,
	 <function>PassThru</function>, <function>popen</function>,
	 <function>EscapeShellCmd</function>, y el <link
	 linkend="language.operators.execution">operador ` (apóstrofe
         invertido)</link>.
	</para>
   </refsect1>
  </refentry>

  <refentry id="function.passthru">
   <refnamediv>
    <refname>passthru</refname>
    <refpurpose>Ejecuta un programa externo y muestra su salida literal
    </refpurpose>
   </refnamediv>
   <refsect1>
    <title>Descripción</title>
    <funcsynopsis>
     <funcdef>string <function>passthru</function></funcdef>
     <paramdef>string <parameter>command</parameter></paramdef>
     <paramdef>int <parameter><optional>return_var</optional></parameter></paramdef>
    </funcsynopsis>
    <para>
     La función <function>passthru</function> es similar a la función
     <function>Exec</function> en que ejecuta una orden
     (<parameter>command</parameter>). Si existe el parámetro
     <parameter>return_var</parameter>, el valor de estado devuelto por
     la orden Unix se guardará ahí. Esta función debería usarse en lugar
     de <function>Exec</function> o <function>System</function> cuando
     la salida de la orden Unix sean datos binarios que deban ser
     pasados directamente al navegador. Un uso típico de ello es
     ejecutar algo como las utilidades pbmplus las cuales pueden dar como
     resultado directamente el flujo de datos de una imagen. Poniendo
     el content-type a <emphasis>image/gif</emphasis> y llamando
     al programa pbmplus para mostrar un gif, usted puede crear
     archivos de órdenes PHP que generen directamente imágenes.</para>
     <para>
	 Véase también <function>exec</function>,
	 <function>system</function>, <function>popen</function>,
	 <function>EscapeShellCmd</function>, y el <link
	 linkend="language.operators.execution">operador ` (apóstrofe
         invertido)</link>.
	</para>
   </refsect1>
  </refentry>

  <refentry id="function.system">
   <refnamediv>
    <refname>system</refname>
    <refpurpose>Ejecuta un programa externo y muestra su salida</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Description</title>
    <funcsynopsis>
     <funcdef>string <function>system</function></funcdef>
     <paramdef>string <parameter>command</parameter></paramdef>
     <paramdef>int <parameter><optional>return_var</optional></parameter></paramdef>
    </funcsynopsis>
    <para>
    <function>System</function> se parece a la versión C de la función
    de mismo nombre en que ejecuta la orden indicada en
    <parameter>command</parameter> y muestra el resultado. Si se indica una
    variable como segundo parámetro, el código de estado devuelto por la
    orden ejecutada se guardará en esta variable.
    </para>
    <para>
    Destacar que si usted va a permitir que se pasen datos provenientes
    de usuarios a esta función, entonces debería usar
    <function>EscapeShellCmd</function> para asegurarse de que los
    usuarios no pueden engañar al sistema para ejecutar instrucciones
    arbitrarias.
    </para>
    <para>
    La llamada a <function>System</function> también intenta vaciar
    automáticamente el buffer de salida del servidor web después de
    cada línea de salida si PHP está funcionando como un módulo del
    servidor.
    </para>
	<para>
	 Devuelve la última línea de la orden en caso de éxito, y
	 falso en caso de fallo.
	 </para>
    <para>
    Si necesita ejecutar una orden y obtener de vuelta todo los datos del
    mismo sin interferencias, use la función <function>PassThru</function>.
    </para>
    <para>
	 Véase también <function>exec</function>,
	 <function>PassThru</function>, <function>popen</function>,
	 <function>EscapeShellCmd</function>, y el <link
	 linkend="language.operators.execution">operador ` (apóstrofe
         invertido)</link>.
	</para>
   </refsect1>
  </refentry>

 </reference>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
sgml-parent-document:nil
sgml-default-dtd-file:"../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
-->
